# 章节与星图版验证记录

2026-09-18。对应本轮课程重构；不代表所有本地复现任务已完整训练。

## 内容与状态

- 8 章、30 课题、95 环节、84 知识点。先修图无循环；所有课题、环节、先修、练习目的地均可解析。
- 10 个 GitHub 仓库固定到提交，环节的文件属于实际阅读过的清单。来源与许可见 `src/atlas/sources.json`。
- 新增 24 个 Python 小算例实际执行；另外检查关键独立数值，如有限差分、KL、前向加噪、SAC 目标及检索评估。
- 原有 60 节内容、学习场景、对象引用实验、课程记录和银行模型校验通过。旧备份、新知识笔记、课题记录均可往返；无效字段和超范围索引经过过滤。
- 银行模型的 6 项 Python 测试通过，包括留出标签隔离、梯度检查和恢复。浏览器默认训练实际得到验证损失约 0.3247。

## 浏览器验证

通过 Codex 内置浏览器测试本地开发版及 /Nicoding/ 子目录的生产构建。

- 课题环节 → 知识页 → 原环节，记录和勾选刷新后保留。
- 自动微分 Python 示例输出 `7.0 7.0`；错误选项有解释，正确作答与运行、记录满足后才允许登记局部完成。
- 星图搜索 SVM、无结果提示、领域筛选、节点文字点击、键盘 Enter 选择、缩放及复位。
- 390×844 和 320×700 手机布局；章节与知识页没有横向页面溢出。手机领域星图两列排列，并提供当前节点的讲解入口。
- 桌面 1536×1024 检查章节、星图、课题、知识四种界面；控制台未发现应用错误。
- 生产构建八章逐页核对，课题数量为 3/3/3/2/6/6/3/4。计算图输入改为 3.5 后，前向值变为 15.75，导数变为 8。
- 课题 Markdown 已实际下载并读取，包含提交、克隆/切换版本命令、五个环节及知识链接。浏览器自动化的下载事件未返回，但磁盘文件证实下载成功。
- 正文滚动 1121px 后，顶栏仍在 y=0，左栏和页标题仍在 y=72；目标实验标题可见。

## 与概念图的视觉核对

概念图通过本地图片查看；实际页面通过浏览器截图逐页比对。

| 核对项 | 实际结果 |
| --- | --- |
| 页面主体 | 顶栏、左侧导航、主体；知识和课题有右侧相关内容，保持相同层级 |
| 版面尺寸 | 桌面 72px 顶栏、218px 侧栏；窄屏切换横向目录 |
| 字体 | 正文统一 Noto Sans SC 子集，新增字符重新生成；保留已有字标 |
| 色彩 | 暖白主体、白色侧栏、深灰正文、赤陶强调色，无整页渐变 |
| 信息密度 | 章节使用开放列表；课题的任务、源码、验收、记录分段，避免层层卡片 |
| 图与交互 | 概念图的静态示意改为真实先修图和可操作计算图；星图长标签裁切与手机点击问题已修复 |

有意差异：数据、节点、GitHub 仓库和运行方式以真实目录为准；移动端图布局重排；不采用概念图中不正确的领域高亮。保留 Nico 现有图像，学习界面默认收起以免遮挡目录。

## 范围

新增 GitHub 课题是可跟随的复现指南，已检查对应源码，但尚未逐个安装所有训练环境、下载数据并完成全程训练。浏览器实际运行部分与本地任务在页面上分开标注。当前仍使用本地浏览器保存，没有账户同步。
