# Nicoding：阅读与布局调整（2026-09-10）

## 范围

- 首页保留继续学习和三组路线；8 个方向通过同样的行式入口展示。
- 课程页保留步骤导航和一篇正文，先修默认折叠，笔记与资料在弹窗中打开。
- 13 个项目先在目录选择，当前项目页只展示本项目的需求、编辑器和记录。
- 21 节课程重写概念讲解，39 节补充具体例子或计算过程；所有课程调整简介。解释练习缩短操作文案，进阶题标为选学。
- Python 前两节改为运行和观察，避免尚未学函数便要求写函数；入门示例与正文使用相同的单价、数量场景。

## 内容检查

`node scripts/validate-content.mjs`：60 节课程、8 条路径、60 份解释练习、141 道理解题、13 组代码挑战（45 条检查）、8 个小项目（30 条检查）、46 个资料入口，错误列表为空。

新增正文中 27 个代码块另行检查语法；23 个不依赖外部文件、网页或接口的代码块实际执行成功。文件读取、DOM、HTTP 和 React 的 4 个片段需要对应上下文，正文注明使用条件，只进行了语法检查。小型算法实验不等于完成真实模型训练。

本次重点核对了 [Python 变量与表达式](https://docs.python.org/zh-cn/3/tutorial/introduction.html)、[控制流与函数](https://docs.python.org/zh-cn/3/tutorial/controlflow.html)、[MDN 闭包](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Guide/Closures)、[React 状态更新队列](https://react.dev/learn/queueing-a-series-of-state-updates)、[PPO 代理目标](https://spinningup.openai.com/en/latest/algorithms/ppo.html) 和 [D2L 的 Q/K/V 说明](https://d2l.ai/chapter_attention-mechanisms-and-transformers/queries-keys-values.html)。课程仍保留对应原文入口，没有把教材整站复制进项目。

## 浏览器检查

使用 Codex 内置浏览器访问本地 Vite 服务。

- 8 个方向路由均加载，分别覆盖 8/8/8/8/6/7/8/7 节；每节数量与课程配置一致。
- 首页 → 方向 → 独立课程、目录 → 项目、关键词搜索 → PPO 课程可操作。
- Python 实验输出可见；完成观察和正确作答后，点亮按钮可用。未保存测试用的完成记录。
- JavaScript 小项目的初始错误被检查捕获；参考实现通过 3 条检查；随后恢复初始代码。
- 笔记输入后刷新保留，键盘清空后刷新保持为空。
- 课程辅助弹窗只暴露弹窗内容，关闭后恢复焦点；修复设置弹窗关闭后 Nico 仍被设为 inert 的问题，并实际重新打开 Nico 面板验证。
- 检查 390×844 和 360×800 手机视口；首页、方向页、正文与项目页正常重排，所检查页面无整页横向溢出。代码块和学习步骤可在自身区域横向滚动。
- 桌面在 1330×1182、1505×1045 及默认窗口检查。浏览器错误和警告日志为空。

## 视觉核对

实现前为首页和课程页生成了结构参考，交付页面使用真实组件。使用 `view_image` 对比参考图与浏览器的最终视口截图。

| 核对点 | 实现 |
| --- | --- |
| 色调 | 米白背景、深棕正文、焦糖主按钮，移除重复的强装饰层 |
| 首页层次 | 一个继续学习入口，三组纵向排列的学习路线 |
| 课程层次 | 步骤栏、标题与辅助入口、正文；移除重复正文的第三列 |
| 字体 | 原有 Nicoding 字标，中文统一 Noto Sans SC；代码等宽并单独排版 |
| 间距 | 统一内容宽度和分隔线，手机减少页头留白 |
| 交互 | 优先节点保留局部效果；选择、展开、弹窗和步骤切换提供反馈 |

与生成参考的有意差异：保留原有获用户认可的 logo 和金白小猫；页首共用简洁搜索按钮；正文使用更窄的阅读宽度；基础概念加入代码块和执行解释后自然延长页面；手机使用底部导航。没有为了复刻概念图压缩课程文字。

## 限制

手机检查是浏览器视口模拟，未在微信或真实手机键盘环境测试。本次验证能确认交互、代码和部分公式行为，不能证明实际教学效果。生产构建可通过，主包略超过 Vite 默认的 500 kB 提示阈值，压缩后约 191 kB。
