import {K,SK} from './knowledge-schema.js';
export const mlKnowledge=[
 K('ml-preprocessing','特征预处理与 Pipeline','把数据变换作为模型的一部分，防止训练与推理脱节。',['ml-splits','py-json'],[
 ['预处理也会学习参数','均值、方差、填补中位数和类别词表都来自数据。它们和模型权重一样，只能由当前训练集估计。验证和测试阶段应用已经拟合的变换；不能为了“让两边分布一样”分别标准化。\n\n数值列、类别列、ID 和标签的语义不同。ID 通常不应直接当连续数值，缺失也不总等于零。先写数据字典，再决定变换。'],
 ['变换与形状','标准化 z=(x−μ)/σ，参数来自训练样本，零方差列需明确处理。One-hot 将类别变成多个 0/1 列；未见类别应有约定，例如全零或 unknown，而不是部署时临时扩大输入维度。\n\n稀疏编码与连续特征合并后，矩阵列数不再等于原始字段数。保存特征名与排列顺序，才能解释权重并保证预测一致。'],
 ['管道与交叉验证','Pipeline 让预处理在每折训练部分重新拟合，然后对该折验证部分只做 transform。先对全量数据 fit_transform 再验证模型，会把验证分布带入训练。\n\nColumnTransformer 组织不同列的变换，Pipeline 串起变换与模型。它们管理组合和生命周期，不会自动发现预测时点错误。目标编码、特征选择和 PCA 也需要折内拟合。'],
 ['一个可以核对的算例','训练值 [10,20,30] 的均值为 20，按总体方差约定，标准差为 √(200/3)≈8.165。新值 40 的变换结果约 2.449。不能把 40 加回去重算均值，否则预测随当前批次改变。\n\n树模型通常不需要线性缩放，距离模型与带正则的线性模型则对尺度敏感。预处理应与模型一起选择和保存。'],
 ],'import math\ntrain=[10,20,30]\nmu=sum(train)/len(train)\nsigma=math.sqrt(sum((x-mu)**2 for x in train)/len(train))\nprint(round((40-mu)/sigma,4))','输出 2.4495；参数只来自三个训练值。',['管道不能修复来自未来的字段。','fit 估计参数，transform 应用参数。'],['交叉验证的填补均值应来自？',['每折训练部分','全量数据','测试集'],0,['每折独立拟合。','包含验证分布，会泄漏。','测试集不能拟合。']],[SK('modules/compose.html','Pipeline 与组合估计器')]),
 K('ml-validation','交叉验证与模型选择','把参数估计、超参数选择和最终评估分开。',['ml-splits','ml-metrics'],[
 ['验证的对象是整个流程','交叉验证用多个训练/验证划分观察一个学习流程，包括预处理、特征选择和模型。它不是拿已在全量数据上训练的模型去各折评分。\n\nK 折降低对一次划分的依赖，但各折训练集重叠，折间分数不是严格独立的重复实验。均值和标准差描述波动，不能随意套独立样本公式构造置信区间。'],
 ['划分匹配部署','独立样本分类可考虑分层 K 折；重复主体需要分组；预测未来需要时间切分。分层只维持标签比例，不解决主体重复、时间穿越和标签构造错误。\n\n面对新门店时，应让门店整体留出。否则估计的是已知门店的新记录，而不是新门店的泛化。'],
 ['搜索造成选择效应','试过很多配置后，最好的验证分数同时包含能力和偶然优势。它不是无偏的最终泛化估计。保留未参与选择的测试集，或使用外层评估、内层选参的嵌套验证。\n\n提前固定搜索预算、候选范围和评分。最终测试一旦参与改方案，就不再是这一轮的独立检验。'],
 ['比较不只看最大分数','A 的分数为 [.81,.83,.82]，B 为 [.78,.88,.80]，均值同为 .82，波动却不同。不能只保留 B 的 .88。还应核查各折人群、样本量与错误结构。\n\n宏平均、加权平均和合并预测后计算分数可能不同，需要说明聚合方法。'],
 ],"for name,scores in [('A',[.81,.83,.82]),('B',[.78,.88,.80])]:\n    print(name,round(sum(scores)/len(scores),3),round(max(scores)-min(scores),3))",'两组均值为 .82，极差 .02 与 .10。',['最好的验证结果可能有选择偏差。','负损失评分需明确还原符号。'],['调参与最终评价复用同一结果，主要问题是？',['选择偏差','无法训练','必须增加特征'],0,['可能选中偶然优势。','训练仍可执行。','不解决评估污染。']],[SK('modules/cross_validation.html','交叉验证')]),
 K('ml-knn','k 近邻：距离与局部预测','没有显式权重训练的模型，也需要选择与验证。',['math-vectors','ml-preprocessing','ml-metrics'],[
 ['定义与假设','对查询 x，寻找训练集中距离最近的 k 个样本。分类用投票，回归用平均，也可以按距离加权。假设是在所选度量下，近邻的目标相似。\n\n没有迭代训练不等于没有模型选择。距离、k、缩放和权重都会改变预测，训练数据本身就是模型的重要存储。'],
 ['尺度与高维距离','欧氏距离为 √Σ(x_j−z_j)²。金额从元换为分会改变距离中的贡献，进而改变近邻集合。标准化是一种度量选择，不是唯一正确答案。\n\n高维空间中距离可能不易区分，无关维度会淹没有用方向。先检查表示与距离，再优化索引速度。'],
 ['k 的偏差与方差','k=1 容易跟随局部噪声，较大 k 让边界平滑，但也可能跨越真实类别边界。k 需由验证选择；查询训练样本时，不能让样本自己作为近邻来证明泛化。\n\n平票、重复点和零距离需有确定行为。距离加权不能直接除以零。'],
 ['手算与工程代价','点 0→A、2→A、3→B，查询 2.8，距离为 2.8、0.8、0.2。k=1 预测 B，k=3 等权投票预测 A。没有哪种选择在所有数据上必胜。\n\n存储量、每次查询延迟和训练数据隐私也会影响应用选择。近邻模型适合检验一个表示是否有用。'],
 ],"points=[(0,'A'),(2,'A'),(3,'B')]\nnear=sorted(points,key=lambda p:abs(p[0]-2.8))\nfor k in [1,3]:\n    labels=[label for _,label in near[:k]]\n    print(k,max(sorted(set(labels)),key=labels.count))",'k=1 得到 B，k=3 得到 A。',['验证样本不能进入近邻库。','近邻索引不修复不合适的度量。'],['金额从元换为分，未缩放的近邻可能？',['不变','改变','自动识别单位'],1,['尺度参与距离。','该维度贡献会变。','算法不知道单位。']],[SK('modules/neighbors.html','Nearest Neighbors')]),
 K('ml-svm','支持向量机与核方法','用间隔理解分类，再用核扩展边界。',['math-vectors','ml-preprocessing','math-opt'],[
 ['软间隔目标','二分类标签 y∈{−1,+1}，线性分数 wᵀx+b。一种常见目标为 ½||w||²+CΣmax(0,1−y(wᵀx+b))，在权重范数和间隔违规之间权衡。\n\nhinge loss 在分类正确且间隔足够大时为零。支持向量约束决策边界，分数本身不是概率。'],
 ['C 与尺度','该目标约定下，C 增大更重视间隔违规代价。结果可能更贴近训练点，具体取决于数据。缩放与 C 相互影响，必须纳入同一验证管道。\n\n库之间的损失求和、平均和正则约定可能不同，不能把相同数值当成相同强度。'],
 ['核函数','核 K(x,z) 在不显式展开高维特征时计算内积关系。RBF 核 exp(−γ||x−z||²) 的 γ 控制局部影响范围，γ 越大影响越局部。\n\n并非任意相似度都满足标准核方法的条件。核矩阵的时间与空间开销也限制大样本应用。'],
 ['手算损失与概率边界','正样本分数为 1.5、.2、−.4，hinge loss 分别是 0、.8、1.4。第二个虽分类正确，仍未满足单位间隔。\n\n需要概率时另做校准并保留独立评估。decision_function 不是 predict_proba；不能按概率规则解读任意分数。'],
 ],'scores=[1.5,.2,-.4]\nprint([round(max(0,1-s),4) for s in scores])','损失为 [0,0.8,1.4]。',['LinearSVC 与线性核 SVC 的默认目标和求解不同。','核模型需要考虑样本规模。'],['正样本分数 .2 的 hinge loss 是？',['0','.8','1.2'],1,['分对不代表满足间隔。','1−.2=.8。','应使用正标签。']],[SK('modules/svm.html','支持向量机')]),
 K('ml-naive-bayes','朴素贝叶斯','从类条件分布理解一个文本分类基线。',['math-prob','ml-splits'],[
 ['条件独立的含义','贝叶斯公式给出 P(y|x)∝P(y)P(x|y)。朴素假设是在给定类别后，各特征条件独立，从而 P(x|y)=∏P(x_j|y)。这不是说特征在原数据中独立。\n\n取对数把乘积变成求和，也减少小概率连乘导致的下溢。'],
 ['变体由数据决定','GaussianNB 用类内高斯描述连续特征；MultinomialNB 常用于非负词频；BernoulliNB 关注是否出现。必须检查输入取值是否满足变体假设。\n\n条件独立常不完全成立，分类仍可能有效，但不能因此认为概率已经校准。'],
 ['平滑与未见事件','某个词在一个类别没出现时，未经平滑的似然为零。加性平滑把计数 c 改为 c+α，同时把分母总计数 N 改为 N+αV，其中 V 是词表大小。\n\nα 需要验证；词表、词频与分词规则都只能由训练集确定。'],
 ['算例','一个类别总词数 10，词表大小 4，某词计数 0。α=1 时概率是 1/14。分母不能只加 1，因为每个词都得到一个伪计数。\n\n部署时新词和领域迁移会改变统计。保存词表、分词器与模型版本。'],
 ],'import math\np=(0+1)/(10+1*4)\nprint(round(p,6),round(math.log(p),6))','概率 .071429，对数概率 −2.639057。',['MultinomialNB 不适合任意含负值特征。','分类有效不证明条件独立为真。'],['朴素假设指？',['给定类别后条件独立','类别相同','词频相同'],0,['这是似然分解依据。','类别可以不同。','词频由数据估计。']],[SK('modules/naive_bayes.html','Naive Bayes')]),
 K('ml-boosting','梯度提升：从残差到函数更新','每一个新学习器，都修正当前模型尚未解决的部分。',['ml-trees','math-grad','ml-validation'],[
 ['加法模型','F_M(x)=F_0(x)+Σηf_m(x)。第 m 轮依赖当前预测来训练新学习器。它和并行训练后平均的 Bagging 有不同的依赖结构。\n\n弱学习器不一定是树；树因能表达局部非线性与交互而常用。'],
 ['负梯度信号','每个样本上计算 r_i=−∂ℓ(y_i,F(x_i))/∂F，新学习器逼近这些值。平方损失 ½(y−F)² 下，负梯度恰好是 y−F。\n\n分类、排序损失不能都用普通残差代替。二阶提升还使用 Hessian，叶子值与正则方式需看具体实现。'],
 ['复杂度与早停','学习率、树数、叶子复杂度、最小叶样本和采样共同影响结果。小学习率往往需要更多轮，因此固定轮数不等于固定算力。\n\n早停轮数来自验证集，最终测试不能选轮。验证的时间或分组边界仍要匹配部署。'],
 ['两轮算例','目标 [1,3]，初始预测 [2,2]，残差 [−1,1]。假设学习器正好拟合残差，η=.5，更新到 [1.5,2.5]。再修正一轮得到 [1.25,2.75]。\n\n真实弱学习器只能近似负梯度，不能把这个两点算例当成完整树训练。'],
 ],'y,pred=[1,3],[2.,2.]\nfor _ in range(2):\n    r=[a-b for a,b in zip(y,pred)]\n    pred=[p+.5*v for p,v in zip(pred,r)]\n    print(pred)','依次得到 [1.5,2.5] 与 [1.25,2.75]。',['训练持续改善不保证泛化。','XGBoost、LightGBM、CatBoost 的实现与类别处理不能混为一谈。'],['一般损失下新学习器近似什么？',['负梯度','乱序标签','测试误差'],0,['它是局部改善方向。','会破坏学习信号。','不能在测试集训练。']],[SK('modules/ensemble.html#gradient-boosting','Gradient Boosting')]),
 K('ml-pca','PCA：投影、方差与重建','用正交方向压缩数据，明确丢弃的内容。',['math-vectors','ml-preprocessing'],[
 ['优化目标','中心化数据 X∈R^(n×d)，找 k 个正交方向 W，使 XW 方差最大，等价于最小化固定维数的平方重建误差 ||X−XWWᵀ||²。\n\n主轴可由协方差特征分解或 X 的 SVD 得到。主轴整体反号不改变子空间，不是实现错误。'],
 ['中心化与标准化','中心化减均值，标准化还除以尺度。PCA 关注方差，因此单位会影响结果。是否缩放取决于大方差是有用信号还是单位差异。\n\n均值与主轴在训练集拟合，新样本使用相同变换。'],
 ['解释方差的边界','95% 解释方差不等于 95% 标签信息或准确率。一个低方差方向可能决定类别，PCA 不使用标签挑选方向。\n\n用于建模时需要检查下游验证指标；二维投影图也不能证明存在真实业务群体。'],
 ['可手算的数据','点 [1,1]、[2,2]、[3,3]，均值 [2,2]，主轴 (1,1)/√2。投影 [−√2,0,√2] 可无损重建，因为数据只有一个变化方向。\n\n加入垂直方向噪声后，一维重建会丢掉它。一般数据并不都能无损压缩。'],
 ],'import math\naxis=[1/math.sqrt(2)]*2\nfor x in [[1,1],[2,2],[3,3]]:\n    z=sum((a-2)*w for a,w in zip(x,axis))\n    print(round(z,4),[round(2+z*w,4) for w in axis])','投影 −1.4142、0、1.4142，重建恢复原点。',['不要将主轴符号当稳定标签。','PCA 是线性方法。'],['解释方差 95% 指？',['准确率 95%','所选度量下保留 95% 方差','95% 样本正确'],1,['未使用标签得出此数。','正确，方差不是任务性能。','PCA 本身不分类。']],[SK('modules/decomposition.html#pca','PCA')]),
 K('ml-dbscan','DBSCAN：密度、连通与噪声','不先指定簇数，用局部密度连接样本。',['ml-unsupervised','ml-preprocessing'],[
 ['核心、边界和噪声','ε 邻域样本数达到 min_samples 的是核心点，常见实现计入自己。靠近核心点但自身密度不足的是边界点，其余可能被标为噪声。\n\n核心点的密度可达关系形成簇，能表达非球形结构而无需指定 K。'],
 ['参数与尺度','ε 是邻域半径，min_samples 是密度门槛。ε 太小会有很多噪声，太大可能合并多个群。缩放影响距离，因此也影响密度。\n\n不同区域密度差异大时，一个全局 ε 难以同时适配。高维距离的区分能力也是限制。'],
 ['标签不等于业务真相','−1 只表示当前参数下未归簇，不证明是欺诈或异常客户。算法没有自然通用的 predict，部署新样本需要额外定义规则。\n\n边界点如果可连接不同簇，其归属可能受访问顺序影响。簇编号没有稳定的等级含义。'],
 ['一维算例','[0,.1,.2,2]，ε=.15，min_samples=3，计入自身。.1 是核心点；0 和 .2 是边界点；2 是噪声。ε 改为 .05 后全部成为噪声。\n\n发生变化的是密度定义，不是数据的客观身份。'],
 ],"xs=[0,.1,.2,2]\nfor x in xs:\n    count=sum(abs(z-x)<=.150000001 for z in xs)\n    print(x,count,'core' if count>=3 else 'not core')",'只有 .1 是核心点，其余仍需判断与核心点的邻接。',['噪声不一定要删除。','分群要报告簇大小与参数敏感性。'],['噪声标记表示？',['已证明业务异常','该密度规则下未归簇','必须删除'],1,['算法不给业务真相。','依赖距离与参数。','删除需另有依据。']],[SK('modules/clustering.html#dbscan','DBSCAN')]),
 K('ml-calibration','概率校准与可靠性','把排序好与概率可信分开检验。',['ml-metrics','math-prob','ml-validation'],[
 ['校准的含义','被预测为 .8 的一组样本中，约 80% 最终为正，符合该区域的校准直觉。这是群体频率性质，不确定单个样本的结果。\n\n保持排序的变换可能不改变 AUC，却改变概率以及按概率计算的收益。'],
 ['可靠性图与评分','将预测分箱，对照箱内平均预测与正例比例。箱数、分箱方法和样本量都会影响图，稀疏箱不能强推结论。\n\nBrier score 是平均 (p−y)²；log loss 更严厉惩罚自信错误。二者衡量综合概率质量，不是纯校准误差。'],
 ['校准器需要独立数据','sigmoid 与 isotonic 等方法从分数学习概率映射。不能随意在模型已经拟合的同一批数据上校准并宣称独立评估。\n\n可用交叉验证的样本外分数，或另留校准集；仍需最终评估。分布变化后，原校准关系未必成立。'],
 ['小样本算例','预测四次 .8，标签 [1,1,1,0]，观察比例 .75，Brier score=(3×.2²+.8²)/4=.19。四个样本不足以证明普遍校准。\n\n阈值仍依赖错误代价和基准率，不能因为校准过就固定 .5。'],
 ],'p=[.8]*4\ny=[1,1,1,0]\nprint(sum(y)/len(y))\nprint(round(sum((a-b)**2 for a,b in zip(p,y))/len(y),4))','比例 .75，Brier score .19。',['校准集属于模型选择资源。','可靠性图需要每箱样本量。'],['AUC 不变，概率校准可能变化吗？',['可能','不可能','AUC 就是校准'],0,['排序不等于概率。','单调变换可改变概率。','AUC 衡量排序。']],[SK('modules/calibration.html','概率校准')]),
 K('ml-interpret','模型解释与置换重要性','解释模型依赖什么，避免误写成因果。',['ml-trees','ml-metrics','math-stats'],[
 ['先定义解释对象','全局解释描述模型在一批数据上的行为，局部解释描述某次预测。解释依赖模型、分布和方法约定，不是世界唯一的真相。\n\n重要特征可能只是代理变量，不能直接推出干预它会改变业务结果。'],
 ['置换的过程','在留出数据上先评分，再打乱一列重新评分。差值衡量破坏这列信息后模型的表现变化。重复置换用来观察随机波动。\n\n评分函数定义“重要”的含义：整体 accuracy 不敏感的特征，也可能关系到稀有群体的召回。'],
 ['相关性与不现实样本','两列几乎重复时，打乱一列还有另一列提供信息，单列重要性可能偏低。独立置换还可能产生现实中不存在的组合。\n\n树的 impurity importance 可能偏向分裂点多的特征。解释前先确认模型在留出数据上确实有效。'],
 ['从差值写结论','基准 .80，置换后 .71、.73、.72，平均下降 .08。可说模型在此数据与指标下依赖该信息，不能说“干预变量让业务增长 8%”。\n\n记录模型版本、数据范围、指标、重复次数和相关变量情况。'],
 ],'baseline=.80\nscores=[.71,.73,.72]\nprint(round(baseline-sum(scores)/len(scores),4))','评分下降 .08，单位与原指标相同。',['先验证模型，再解释模型。','SHAP 等方法也有基线与相关性假设。'],['置换后下降 .08 支持？',['干预增长 8%','此条件下模型依赖该信息','证明因果'],1,['不支持因果外推。','这是对应实验支持的结论。','预测依赖不是因果证据。']],[SK('modules/permutation_importance.html','置换重要性')]),
 K('ml-timeseries','时间特征与滚动验证','围绕预测时点构造输入，避免未来信息混入。',['ml-splits','py-containers','ml-validation'],[
 ['样本有时间语义','用 t 日结束信息预测 t+1 日销量，输入最多包含 t 及以前的数据。标签窗口、数据入库延迟与特征窗口都需记录；当天有一列不等于预测时已经有它。\n\n相邻样本往往相关，随机打乱切分不能直接估计预测未来的效果。'],
 ['滞后与滚动','预测目标日期 t，lag_1 是 y_(t−1)，过去三天均值是 (y_(t−1)+y_(t−2)+y_(t−3))/3。通常先 shift(1)，再 rolling(3)，避免目标进入特征。\n\n事先已知的节假日可用；事后销量、退款和补录信息不可混入。'],
 ['滚动协议','expanding window 保留越来越多历史，sliding window 只保留最近一段。训练部分在验证目标之前。跨界标签窗口或数据延迟可能需要 purge 或 gap。\n\ngap 大小来自数据生成与预测机制。逐窗报告误差，均值可能掩盖漂移。'],
 ['基线和指标','先比较昨日值、上周同日值等简单基线。MAE 保持原单位，MAPE 在目标接近零时会有问题。\n\n合成数据的低误差只表明抓住了生成规律；真实部署还要面对缺测、结构变化和标签延迟。'],
 ],"y=[10,12,11,16,14]\nfor t in range(3,len(y)):\n    print('t',t,'prediction',round(sum(y[t-3:t])/3,2),'actual',y[t])",'t=3 预测 11，t=4 预测 13，未包含当前目标。',['时间验证的预处理也只拟合历史。','回测准确不证明干预有效。'],['预测 y_t 的过去三天均值包含？',['y_t 与前两天','前三天，不含 y_t','未来三天'],1,['含目标会泄漏。','均为预测前已观测值。','未来不可用。']],[SK('modules/cross_validation.html#time-series-split','时间序列划分')]),
];

