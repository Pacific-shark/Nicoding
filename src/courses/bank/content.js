const s=(title,...paragraphs)=>({title,paragraphs});
const q=(question,options,feedback)=>({question,options,feedback});
export const sources={
 data:['UCI · Bank Marketing 数据与字段','https://archive.ics.uci.edu/dataset/222/bank+marketing'],
 python:['Python 官方教程 · 数据结构','https://docs.python.org/3/tutorial/datastructures.html'],
 leakage:['scikit-learn · 数据泄漏与预处理','https://scikit-learn.org/stable/common_pitfalls.html'],
 model:['scikit-learn · Logistic regression','https://scikit-learn.org/stable/modules/linear_model.html#logistic-regression'],
 metrics:['scikit-learn · 分类指标','https://scikit-learn.org/stable/modules/model_evaluation.html#classification-metrics'],
};
export const content={
 brief:{lead:'同事给你一张历史营销表，希望下一次电话联系更有针对性。先别训练：有些字段在拨号前根本不存在。',goal:'写清预测对象、标签含义和可用时间，识别容易泄漏的字段。',sections:[
 s('先约定预测发生在什么时候','本项目的决策时点是：新一轮电话联系开始前。输入是这一刻已知的客户资料与过往联系记录，输出是历史条件下“会订阅定期存款”的概率分数。y=yes 记为 1，y=no 记为 0。每行是一条记录，不能断言每行都是不同客户。','课程用银行营销记录练习方法。Nico 小店的同事只是任务引导，银行数据没有被改称小店订单。这里复现的是透明的建模流程，不复现原论文的全部结论。'),
 s('duration 为什么不能用','duration 是最后一次通话时长，完整通话结束后才知道，不应输入拨号前的模型。即使加入后分数大幅提高，也可能只是提前看到了结果发生过程中的线索。y 更不能混进输入；它就是要预测的答案。','本实验保留 age、balance、previous 三个数值字段，以及 job、marital、education、housing、loan、poutcome 六个类别字段。campaign 包含当前联系次数，day、month、contact 的可用时点在本练习里未充分约定，因此也不进入模型。真实项目应逐字段核实数据生成流程。'),
 s('这个模型还不能回答什么','高订阅概率不等于“多打一个电话会多带来一笔订阅”。有些人即使不联系也会购买。估计联系的增量效果，需要对照设计或更严格的因果分析。后面的误报、漏报成本是教学假设，不是营销利润。','UCI bank.csv 是原始大表的 10% 随机子集，共 4,521 行。它缺少足够的时间信息与稳定客户标识，本部分只能做同一历史样本分布下的教学评估。上线前还需要时间外验证、数据权限核查与业务试验。')],
 quiz:q('拨号前做预测，哪个方案可以作为起点？',['加入 duration，验证分数最高就保留','只用约定时点前可用的字段，并明确预测不等于增量效果','把 y 放入特征帮助模型学习'],['验证分数不能让未来信息变成可用信息。','对。输入可用性与结论边界，都属于建模约定。','y 是答案，不能放入 X。']),sources:['data','leakage'],bridge:['py-json','ml-splits']},
 rows:{lead:'任务约定好了，下一步是读数据。CSV 中的“30”仍是字符串；“no”也不是 Python 的 False。',goal:'把记录转换为二维输入 X 与一维标签 y，不修改原记录。',sections:[
 s('行、列、样本与特征','csv.DictReader 通常产生字典组成的列表。rows[0] 是第一条记录；rows[0]["age"] 是该记录的年龄字符串。外层列表决定样本顺序，内层字段描述这一条记录。','模型中的 X 是二维结构：N 行样本、D 列特征。y 是长度 N 的标签。X[i] 与 y[i] 必须属于同一记录。只打乱 X、不一起打乱 y，会让模型把别人的答案当成这一行的答案。'),
 s('三个可能不报错的错误','bool("no") 是 True，因为非空字符串都是真值。标签应显式比较 row["y"] == "yes"。列表乘法也会共享内部对象：[[0,0]] * 3 不是三份独立小列表，改一行可能一起变。','函数如果直接给原字典删除 y，调用者再次使用记录时就会丢字段。本题只创建新列表，输入保持原样，测试会核对这一点。')],
 code:'rows = [{"age":"30", "balance":"120", "y":"no"}]\nX = [[30.0, 120.0]]  # 1 行、2 列\ny = [0]              # 第 0 行的答案\n# X[0][1] 是余额；y[0] 是这一行的标签',
 quiz:q('哪个表达式能正确编码 yes/no？',['int(bool(row["y"]))','row["y"] != ""','int(row["y"] == "yes")'],['yes 和 no 都是非空字符串，都会变成 1。','这只检查是否为空，没有区分两类。','对。明确比较标签，也便于读入时校验异常值。']),sources:['python'],bridge:['py-values','py-containers','py-functions']},
 split:{lead:'如果先拿整张表算均值，再切成三份，测试数据已经参与了训练准备。',goal:'区分 fit 与 transform；修复用验证数据重新计算均值的错误。',sections:[
 s('三份数据，三个职责','训练集用于拟合参数，也用于估计标准化的均值、标准差和类别词表。验证集用于比较学习率、特征和阈值。最终测试集用于方案冻结后报告结果，不应成为日常调参面板。','切分发生在所有会“从数据学习”的步骤之前。把 yes 固定编码为 1，不会估计总体规律；全表均值、填补规则、特征选择会。前者可以直接应用，后者必须只在训练集拟合。'),
 s('本课怎样分配记录','相同的九个候选输入字段组成同一个分组。对分组字符串做 SHA-256 哈希，按固定规则分到约 70% 训练、15% 验证、15% 测试。重复的可见输入不会跨集合；这不是客户 ID 去重，因为原表没有可靠客户 ID。','哈希输入不含 y 或 duration。更换特征开关不会重划样本，因此实验才可比较。比例是近似值，没有强行重采样凑整数。训练页会列出实际数量。'),
 s('一套转换规则，应用到不同数据','训练年龄 [20,40] 的均值是 30，总体标准差是 10。验证年龄 50 应变成 (50−30)/10=2。不能看到 50 就重新拟合，让它变成 0；这会改变坐标系，也无法复用训练规则。','本课未做时间外评估。不同输入组仍可能来自同一个人。以后拿到时间戳和客户标识，应按部署场景重做切分。')],
 quiz:q('新验证样本应该怎样标准化？',['复用训练均值与标准差，只做转换','拼进训练数据重新计算均值','验证集单独拟合一套标准化器'],['对。转换参数也是交付的一部分。','验证分布进入了拟合；看不到标签也可能泄漏。','模型系数对应训练坐标系，两套坐标系不一致。']),sources:['leakage','data'],bridge:['ml-splits','py-functions']},
 baseline:{lead:'同事说“准确率接近九成了”。先问：如果所有记录都预测为不订阅，会得到多少？',goal:'读出两类错误，知道为什么要保留朴素基线。',sections:[
 s('做一个没有特征的模型','概率基线只在训练集计算订阅比例 r，然后对每条验证记录都输出 r。它不读取年龄、职业、余额。训练分布里正类较少时，用 0.5 阈值就会全部预测为负类。','准确率可能很好看，召回率却是 0。没有样本被选中时，精确率分母为 0，应显示“无定义”。训练工作台会同时保留基线与模型的验证损失。'),
 s('把四个格子说成人话','TP：实际订阅，也被选中。FP：实际未订阅，却被选中。FN：实际订阅，却没被选中。TN：实际未订阅，也未被选中。精确率 TP/(TP+FP) 问“选中的有多准”；召回率 TP/(TP+FN) 问“正类找回了多少”。','下方 100 条记录是刻意做的小例子，不是 UCI 运行结果。拖动阈值，观察准确率与召回率。真实样本表会在训练后出现。'),
 s('选指标要对准问题','本部分用 log loss 比较概率预测，用精确率、召回率和混淆矩阵描述阈值下的决策。后续继续学习 PR、ROC、校准和不同切分下的不确定性；一次验证分数不能代表这些问题都解决了。')],
 quiz:q('100 条有 10 条正类，全部预测负类会怎样？',['准确率和召回率都是 90%','准确率 90%，召回率 0%，精确率无定义','准确率 0%，精确率 100%'],['准确率看所有判断；召回率只看真实正类。','对。多数类基线揭示了高准确率的局限。','90 条负类预测正确，但没有预测正类可算精确率。']),sources:['metrics'],bridge:['ml-metrics','math-prob']},
 score:{lead:'基线给所有人同一分数。现在希望输入不同，分数也能不同，先从一个特征开始。',goal:'读懂 z=x·w+b 与 sigmoid，区分分数和决策阈值。',sections:[
 s('先加权求和，再映射到 0 与 1','x 是预处理后的一行输入，w 是各特征系数，b 是偏置。z=x·w+b 把输入合成一个实数。sigmoid(z)=1/(1+exp(−z)) 把它映射到 0 与 1 之间。z=0 时 p=0.5；z 越大，p 越接近 1。','下方只用一个标准化特征。w 改变方向与陡峭程度，b 让曲线平移。正式训练由数据决定 w 和 b，不是手动把曲线拖到顺眼。'),
 s('概率分数不是承诺','逻辑回归以条件概率模型形式输出分数，但 p=0.8 不自动意味着类似客户恰有 80% 订阅。假设、分布与拟合误差影响校准，需要额外检查。','阈值是另一件事。p≥t 才选中。把 t 从 0.5 改到 0.2，不会修改 w，也不会让任何样本的 p 变化。'),
 s('为什么仍叫线性模型','sigmoid 是非线性的，但固定阈值对应 x·w+b 等于常数，原特征空间的决策边界仍是线性的。本部分不加入交互项；如果“年龄与过往联系次数的组合”才有用，简单边界可能表达不好。')],
 quiz:q('固定权重，阈值从 0.5 降到 0.2，会怎样？',['模型重新学习权重','所有样本的 p 都变大','p 不变，被选中数量可能增加'],['改阈值不是训练。','p 由输入和模型决定。','对。选中集合扩大或不变；精确率不保证单调变化。']),sources:['model'],bridge:['math-vectors','math-prob']},
 loss:{lead:'只看分类对错，很多参数变化都得不到反馈。训练需要一个连续的目标。',goal:'解释交叉熵如何惩罚自信的错误，理解 L2 项。',sections:[
 s('真实是 1，分数越低，代价越大','单条损失 ℓ=−[y·ln(p)+(1−y)·ln(1−p)]。y=1 时只剩 −ln(p)；p 从 0.5 升到 0.9，损失从约 0.693 降到 0.105。若 y=0 且 p=0.99，损失约 4.605：这是自信的错误。','训练对所有样本取平均。本课没有类别加权，少数类仍可能难识别。调阈值会改变取舍，却不会补上缺失特征或凭空提高排序能力。'),
 s('优化目标和图中曲线的口径','本实验优化“平均交叉熵 + λ·||w||²/2”。L2 抑制过大权重，偏置 b 不受惩罚。λ 是选择的超参数，w、b 是训练得到的参数。λ 太大也可能欠拟合。','train 与 valid 曲线只显示预测损失，不含 L2，便于同口径比较。实际代码使用 logaddexp 避免极端 z 上 exp 溢出。下方滑块用于解释公式，不是完整的数值稳定实现。'),
 s('训练损失下降还不够','梯度只使用训练数据，验证损失未必一起下降。曲线分叉可能提示过拟合、分布差异或训练设置问题。验证集反复用于选择后也会间接影响方案，因此还要保留测试集。')],
 quiz:q('真实 y=0，预测 p=0.99，交叉熵如何？',['很大，因为非常自信地预测错了','接近 0，因为概率接近 1','和 p=0.51 完全一样，反正都错'],['对，log loss 保留置信程度的信息。','要结合真实标签看。','分类都错，但错误的自信程度不同。']),sources:['model','metrics'],bridge:['math-opt']},
 gradient:{lead:'把训练拆成可执行的一步：预测、算误差、算梯度、更新参数。',goal:'实现单特征批量更新，检查平均值、符号与正则项。',sections:[
 s('从单条误差到整批梯度','逻辑回归配合交叉熵，关于 z 的导数正好为 p−y。这不是所有模型通用的公式，而是这组模型和损失组合求导后的结果。对 w 再乘 x，对 b 直接保留 p−y。','N 条样本取平均：grad_w=mean((p−y)·x)+λw，grad_b=mean(p−y)。然后 w←w−η·grad_w，b←b−η·grad_b。用减号，因为梯度指向局部增大的方向。'),
 s('手算一轮再写程序','x=[1,2]、y=[0,1]、w=b=0 时，p 都为 0.5。grad_w=(0.5×1−0.5×2)/2=−0.25，grad_b=0。η=0.2 时新 w=0.05，新 b=0。正类的 x 更大，这步提高了较大 x 的分数。','用同一份旧参数算完整个梯度，再同时更新；中途改 w 会让后面样本用不同版本。本课是全批量，以后学 SGD、mini-batch 时再改变每次读取多少样本。'),
 s('检查比“能运行”多一步','测试检查正则、平均与输入不变。还可用 [L(w+ε)−L(w−ε)]/(2ε) 近似导数，核对解析梯度。ε 太小会受浮点舍入影响。实际训练代码也会用有限差分检查。')],
 code:'error = p - y\ngrad_w = X.T @ error / len(y) + l2 * w\ngrad_b = error.mean()\nw = w - learning_rate * grad_w\nb = b - learning_rate * grad_b',
 quiz:q('η 与 w 的角色是什么？',['都由训练自动计算，不用选择','η 控制步长；w 由梯度更新逐步学到','w 控制步长；η 是输入的系数'],['本课 η 是实验设置。','对，优化设置与模型参数不同。','角色反了。']),sources:['model'],bridge:['math-grad','py-functions']},
 features:{lead:'数字可以乘权重，“职业是学生”却不能直接参与乘法。编码决定模型能读到什么。',goal:'解释标准化与 one-hot，理解未知类别的处理。',sections:[
 s('数值列先标准化','年龄几十，余额可能几千或为负。尺度影响梯度大小，同一个学习率可能难以同时推进。训练集标准化 x′=(x−μ)/σ 让数值列处在更接近的尺度。σ=0 时本课使用 1。','标准化不会消除异常值。极端余额仍会影响梯度；以后可比较稳健缩放或固定变换。本课不为了漂亮曲线临时删除难预测的样本。'),
 s('one-hot 不给职业排高低','把学生、管理、服务业编码为 0、1、2，线性模型会误以为有等距关系。one-hot 为训练期出现的每种类别建一列，样本只在对应列填 1。','词表只从训练集建立。新类别对应的整组列全为 0；训练中真实的字符串 unknown 则是正常类别，两者不同。这个约定丢失了新类别之间的差异。'),
 s('系数解释有条件','本课使用完整 one-hot 与独立偏置，有冗余参数；带 L2 可以训练，但单列系数不是因果效应。共线性、编码与正则都会影响解释。','先保持切分、学习率、轮数和 λ 不变，只比较“仅数值”与“数值加类别”。类别没有改善验证损失也是有效结果，不应隐藏那次实验。')],
 quiz:q('验证集出现新职业，本课如何处理？',['加进词表，重建训练输入','按字典顺序编码成更大的数','沿用训练词表，该字段 one-hot 全为 0'],['这会改变训练好的列数与含义。','无序类别不应自动获得大小关系。','对，这是明确的实现约定，仍应监测未知比例。']),sources:['leakage','model'],bridge:['ml-linear','math-vectors']},
 train:{lead:'先写你预计会发生什么，跑一版，再只改一个因素做对照。',goal:'保存两次真实训练，对照相同验证集并解释差异。',sections:[
 s('第一轮怎样做','默认设置跑“数值加类别”，再只关闭类别输入，其他设置不变。先写假设，如“加入过往营销结果和职业，会降低验证损失”。若研究收敛，则固定输入，只比较轮数或学习率。','模型从零权重、零偏置开始，执行全批量梯度下降。每 10 轮记录损失，保存最后一轮参数，不自动挑最低验证损失。每次重新开始，不带入旧权重。'),
 s('怎样读实验记录','切分由固定输入分组哈希决定，训练是确定性的。相同设置重跑可检查复现，但不能冒充单因素对照。记录没有改善的一版，并解释假设为何不成立。','训练在浏览器执行，不调用在线 AI，也不上传记录。首次加载 Python 与 NumPy；离开本节会终止未完成运行。最近四次完成实验保存在浏览器，可随学习进度备份导出。')],
 quiz:q('哪个对照能判断类别特征的作用？',['切分和其他参数不变，只改变类别开关','换测试集，同时提高学习率与轮数','只保留最高分一次'],['对，先隔离变量再解释。','改变太多，无法归因。','失败记录也是信息。']),sources:['leakage','model'],bridge:['ml-splits','math-opt']},
 decision:{lead:'现在回答同事的问题：按什么规则选人，哪里容易错，冻结后表现如何？',goal:'检查验证错误，冻结模型、阈值和成本后评估测试集。',sections:[
 s('代价要先约定','设误报成本 C_FP、漏报成本 C_FN，总假设损失为 FP×C_FP+FN×C_FN，正确判断成本设为 0。这只是练习损失表，不包含真实联系成本、收入与因果增量。','降低阈值通常增加选中数量和召回，精确率不保证每一步都降。点混淆矩阵看具体记录。错误可能来自信息不足、标签噪声、模型限制，不能都归结于阈值。'),
 s('冻结后才打开测试','冻结权重、预处理、阈值和两项成本。测试分数到这一步才计算并呈现。页面会记录你是否看过测试。','测试后再改模型或阈值，新报告标注“探索复测”。这可以帮助学习，但不是新的独立验收。浏览器标记只是学习提醒，不是防作弊系统；可下载数据本来就能自行打开。'),
 s('结论只覆盖被检验的范围','单次测试只是有限样本估计，不能证明跨时间、跨机构有效，也不包含公平性审查与生产监控。交付时把限制和结果一起写，不要直接声称达到上线标准。')],
 quiz:q('看过测试后调阈值，再测同一数据，应怎样描述？',['全新独立验收','同一测试集上的探索复测，独立验收需要新数据','更高分就覆盖旧结果'],['改参数无法恢复独立性。','对，复测可以分析，但结论强度要降低。','覆盖会隐藏选择过程。']),sources:['metrics','leakage'],bridge:['ml-metrics']},
 transfer:{lead:'同事补充：这轮最多联系 k 条记录。固定阈值不保证数量，现在由你改规则。',goal:'实现预算内排序，处理同分、空输入与边界，不修改原记录。',sections:[
 s('把阈值规则换成名额规则','给定 {id,p} 记录，按分数降序选最多 k 条；同分按 id 升序。返回 id 列表，原输入不变。k=0 返回空列表，k 超出数量返回全部，负 k 报 ValueError。','id 顺序仅用于复现，不代表价值高低。按分类分数选前 k 条也不等于按增量收益选前 k 条。真实决策还要约束联系资格、频次与收益口径。'),
 s('独立写，再对照','测试使用独立小样本，检查规则而不是银行数据分数。不要对公开例子硬编码；另造一组全同分、id 乱序的样本。提示和参考使用会被记录。','再想：若要求“最多 k 条，且 p≥0.2”，过滤该放哪里？这个变式没有自动评分，可以在本地包中扩展，把新约定与边界测试一起写下。')],
 quiz:q('同分时用固定 id 顺序是为了什么？',['重复运行得到相同名单','证明 id 小的客户价值高','保证选中者都会订阅'],['对，是同分规则，不是价值判断。','编号没有这种证据。','排序不能把预测变成事实。']),sources:['python'],bridge:['py-functions','py-containers']},
 handoff:{lead:'让没看过你操作过程的人，也能复核实验结果。',goal:'交付可复现参数、测试报告和自己的解释。',sections:[
 s('交付需要哪些文件','实验包包含原 CSV、来源、脚本、NumPy 版本。冻结记录包含权重、预处理、阈值和数据校验值，可在本地复算测试。全站学习备份保存阅读、答题、代码和实验进度，用途不同。','用自己的话说明：任务是什么；为什么不用 duration；两次实验改变什么、观察到什么；为什么选择该阈值；哪个错误值得继续查；哪些结论不能下。至少 100 字只是保证交付空间，系统不会凭字数判定解释正确。'),
 s('讲给没学过的人听','合上代码，解释“一行记录如何变成分数，参数怎样被训练数据改动”。如果说到优化就卡住，回到梯度那节，用两条样本再算一次。费曼式复述用来发现理解断点，不是背术语。','真正掌握仍要靠新数据、新约束下的迁移。本部分完成仅说明本地留下学习证据，不能证明生产能力，也不能检测外部 AI 代写。'),
 s('下一部分保留什么，改变什么','切分、基线、实验记录与错误分析，在决策树、随机森林、梯度提升和深度学习中仍然需要。下一部分将更换模型结构，保留评估约定，比较非线性表达的收益与代价。','现有算法索引与阈值演示可以查阅。完整树模型与深度学习项目课程在后续批次，不在本次完成范围。')],
 quiz:q('哪一份交付便于复核？',['最高准确率截图','一句“模型学会了营销”','数据版本、可运行代码、冻结参数、报告与失败边界'],['截图还原不了流程。','超出了实验支持范围。','对，他人应能重跑，并理解适用范围。']),sources:['data','leakage'],bridge:[]},
};
