export const projectGuides={
 'mini-cart':{tasks:['校验输入','计算折后金额','处理边界情况','补充自己的测试'],checks:['正常折扣计算','空输入与全额折扣','非法输入','输入保持不变与折扣类型'],hints:['先把输入校验写在计算之前。注意 True 在 Python 中也是 int 的子类。','1001 分 × 2 件，减免 10%，按约定向下取整为 1801 分。先求总额，再舍入。','分别检查空列表、缺字段、负价格、布尔数量，以及原输入是否被修改。']},
 'mini-tasks':{tasks:['按 ID 定位任务','生成新的状态','处理未知 ID','接入页面与存储'],checks:['更新目标且保留旧状态','连续切换两次','未知 ID 与空列表'],hints:['用 map 遍历任务。匹配 id 时生成新对象，其他项保留原引用。','新的 done 应与旧的相反；原数组和原任务仍应表示修改前的状态。','未知 id 也要返回新数组。用空数组和不存在的 id 各检查一次。']},
 'mini-report':{tasks:['确定数据粒度','按订单累计金额','覆盖异常与空输入','用 SQL 核对结果'],checks:['重复订单与退款聚合','空输入','拒绝非法记录','保留原始输入'],hints:['用 order_id 做字典的键。第二次遇到同一个订单时累加，而不是覆盖。','退款允许负金额。先确认 amount 是整数，再进行累计。','在 SQLite 中对同样的记录执行 GROUP BY order_id，核对两种实现的总额。']},
 'mini-gradient':{tasks:['写出损失与导数','记录每一步更新','检查参数与边界','观察学习率影响'],checks:['逐步更新与轨迹长度','零步与过大步长','非法参数'],hints:['损失是 (w−target)²，梯度是 2×(w−target)。先在纸上算第一次更新。','history 先包含初值，每次更新后再追加；运行两步应有三个值。','正学习率不保证收敛。试试 rate=1，并解释为什么可能来回跳动。']},
 'mini-metrics':{tasks:['逐条判断预测','累计混淆矩阵','处理零分母与坏输入','比较阈值与代价'],checks:['混淆矩阵与比例','空数据','阈值恰好相等','错误形状与范围'],hints:['列出每条样本的真实标签和 score>=threshold 的结果，再判断是否属于 TP、FP 或 FN。','精确率的分母是 TP+FP，召回率的分母是 TP+FN。按本题约定，零分母返回 0。','检查阈值相等、两个列表长度不同、布尔标签和 NaN。']},
 'mini-neuron':{tasks:['计算每条样本的误差','求平均梯度','同时更新参数','核对数值梯度'],checks:['第一步参数更新','零误差时保持参数','非零初值的平均梯度','训练数据保持不变'],hints:['所有样本的误差都要使用本轮相同的旧 w、b。先计算完整的 errors 列表。','分别求 mean(2ex) 和 mean(2e)，然后再更新两个参数。','如果结果差一个样本数量的倍数，检查用了 sum 还是 mean。有限差分也应对应同一种损失。']},
 'mini-retriever':{tasks:['规范化查询词','按去重交集计分','处理空结果与排序','核对返回的证据'],checks:['去重与大小写','无命中与零数量','同分时保持原序','非法 limit'],hints:['查询先 lower、split，再转成 set。文档中的重复词也不应重复计分。','只保留分数大于零的文档，再按分数降序稳定排序。','limit=0 应返回空列表；负数和 True 都不符合这里的 limit 约定。']},
 'mini-qworld':{tasks:['定义三格环境','分别计算两种目标','更新独立的 Q 表','观察奖励传播'],checks:['终止目标与原值','奖励向前传播','每一行独立复制','一般学习率与折扣'],hints:['done=True 时目标只有 reward。即使终点存着很大的 Q 值，也不能继续加进去。','先更新状态 1 的右移，再更新状态 0 的右移，第二次才能用到刚学到的后续价值。','外层 copy 还不够。每一行也需要新列表，才能在修改新表时保留原表。']},
 calculator:{tasks:['明确规则与单位','实现核心计算','接入 JSON 输入','检查修改与回归','验证启动说明']},
 webapp:{tasks:['定义用户任务','实现状态与存储','完善失败与空状态','验证主要交互','记录用户试用']},
 mlproject:{tasks:['定义样本与时点','切分数据','比较基线与模型','分析错误代价','记录限制与失败']},
 agentproject:{tasks:['整理文档与权限','验证检索','核对回答与引用','设置执行边界','评估质量与成本']},
 rlproject:{tasks:['定义环境','实现基线与 Q 更新','固定评估方式','比较多个随机种子','复现进阶算法']}
};
export const projectFilename=project=>`${project.id.replaceAll('-','_')}.${project.lang==='javascript'?'js':'py'}`;
