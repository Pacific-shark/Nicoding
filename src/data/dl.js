import {lesson as L,question as Q} from './schema.js';
export const dl = [
L('dl-tensors','张量与广播','神经网络的第一张地图是 shape 表。',['math-vectors','ml-linear'],[
 ['张量让数据带上多个轴','标量是零维，向量一维，矩阵二维；图像批次可用 (N,C,H,W)。轴的顺序是约定，不能仅凭元素数量相同就随意 reshape。reshape 重新解释布局，transpose 交换轴。'],
 ['广播从末尾比较维度','维度相等或其中一个为 1 时可扩张。预测形状 (N,1) 与标签 (N,) 相减可能扩成 (N,N)，结果合法但不是逐样本误差。明确 squeeze/unsqueeze 的意图。']
],`pred = [[1], [2], [3]]
target = [1, 2, 3]
# 用列表显式演示意外的两两相减
wrong = [[p[0]-t for t in target] for p in pred]
right = [p[0]-t for p,t in zip(pred,target)]
print(wrong)
print(right)`,'wrong 是 3×3，right 是三个零。这里不用张量库也能看清广播后果。',['元素数相同不意味着轴的含义相同。','批次轴与特征轴弄反，程序可能仍能执行。'],[
 Q('(N,1) 减 (N,) 在常见广播规则下可能得到？',['(N,N)','始终 (N,1)','必定报错'],0,['正确，末尾维度扩张形成两两差。','这需要标签也为 (N,1)。','规则允许这种广播。']),
 Q('reshape 与 transpose 的关系？',['完全相同','一个重解释形状，一个交换轴','都自动理解样本语义'],1,['两者操作不同。','正确，必须结合内存与轴含义理解。','库不知道业务语义。'])
],['torch'],'阅读一段 PyTorch forward，为每一层手写输入输出 shape。'),
L('dl-backprop','自动微分与训练循环','把“模型训练”拆成五个看得见的动作。',['dl-tensors','math-grad','math-opt'],[
 ['一次迭代的责任','准备批次 → forward 得到预测 → 算 loss → backward 得到梯度 → optimizer.step 更新参数。PyTorch 默认累积梯度，需要按策略 zero_grad；验证通常不需要构建求导图。'],
 ['训练模式与求导开关不同','model.eval() 切换 Dropout/BatchNorm 等模块的行为，no_grad() 关闭梯度记录；二者不互相替代。下面标量实现手动梯度，为真实 autograd 教程做准备。']
],`w = 0.0
x, y = 2.0, 6.0
for step in range(5):
    pred = w*x
    loss = (pred-y)**2
    grad = 2*(pred-y)*x
    w -= 0.05*grad
    print(step, round(loss,4), round(w,4))`,'损失下降，w 靠近 3。这里只有一个参数，没有调用自动微分框架。',['忘记清零会使梯度跨步累积，除非这是有意设计。','detach 会切断梯度连接，不能随手用于所有中间值。'],[
 Q('backward 主要完成什么？',['计算梯度','自动下载训练集','必定更新参数'],0,['正确，更新通常由优化器 step 完成。','与反向求导不同。','计算与应用梯度是不同步骤。']),
 Q('eval() 能代替 no_grad() 吗？',['可以','不可以，两者职责不同','都删除模型'],1,['切模式不等于关闭求导。','正确，推理常配合使用。','都不是删除操作。'])
],['torch'],'照 PyTorch Basics 在本地完成一个小训练循环，记录各步输入、输出和梯度。',{depth:'算法进阶',minutes:45}),
L('dl-regularize','优化器与泛化','训练得更好，不一定学得更好。',['dl-backprop','ml-splits'],[
 ['优化与泛化分别观察','SGD 用当前批次估计梯度，动量平滑更新方向，Adam 自适应调整不同参数的更新尺度。优化器没有普遍最优，先固定评估与预算再比较。'],
 ['限制记忆噪声','权重衰减、Dropout、数据增强和早停有不同作用。训练误差下降、验证误差回升是过拟合信号之一。早停选择依赖验证集，最终测试集不能用于挑 epoch。']
],`train_loss = [0.9, 0.6, 0.3, 0.2]
valid_loss = [1.0, 0.7, 0.5, 0.8]
best = min(range(len(valid_loss)), key=lambda i: valid_loss[i])
print("选择 epoch:", best+1)
print("对应训练/验证损失:", train_loss[best], valid_loss[best])`,'按最小验证损失选择第 3 个 epoch；不是训练损失最低的第 4 个。',['Adam 的耦合 L2 与 AdamW 的解耦权重衰减不宜混为一谈。','更大网络不自动解决数据质量问题。'],[
 Q('早停通常用哪个集合选择 epoch？',['验证集','反复使用测试集','只看参数数量'],0,['正确，测试集保留最终评估。','会污染最终估计。','不能说明泛化表现。']),
 Q('训练损失降、验证损失升，首先警惕？',['过拟合','模型已经完美','文件名错误'],0,['正确，也需排查分布与实现差异。','观察恰恰不支持这个结论。','没有这种直接证据。'])
],['torch'],'绘制或记录训练与验证损失，解释你会在哪一步保存模型及理由。',{depth:'算法进阶'}),
L('dl-cnn','卷积与残差网络','从局部模式构建更深的表示。',['dl-backprop'],[
 ['卷积共享局部权重','同一个核滑过不同位置，以较少参数检测局部模式。步幅控制滑动距离，填充影响边界与输出大小。多数深度学习库名为卷积的操作实际采用互相关。'],
 ['残差给信息一条捷径','残差块写作 y=F(x)+x，让一部分信号和梯度直接穿过。相加要求形状兼容；维度变化时常用投影。残差有助于训练深层网络，不保证任意加深都更好。']
],`x = [1, 2, 3, 4]
kernel = [1, -1]
out = [sum(x[i+j]*kernel[j] for j in range(2)) for i in range(3)]
print(out)
F = lambda v: v*0.1
print([F(v)+v for v in x])`,'无填充、步幅 1 的互相关得到 [−1,−1,−1]；残差输出是输入加分支结果。',['卷积具有位置相关的结构假设，不是所有表格任务的优先选择。','相加前要核对通道和空间尺寸。'],[
 Q('残差块的典型形式？',['F(x)+x','F(x)−所有数据','只保留标签'],0,['正确，直接路径与变换分支相加。','不是残差连接定义。','不构成网络计算。']),
 Q('卷积权重在空间位置上共享，意味着？',['不同位置复用同一核','每个位置必须独立一套参数','完全不需要训练'],0,['正确，这是参数共享的重要来源。','这更像非共享局部连接。','核的参数通常仍需学习。'])
],['resnet','d2l'],'手算一个卷积的输出形状，再在 PyTorch 中验证步幅与填充的影响。',{depth:'算法进阶',minutes:45}),
L('dl-sequence','RNN、LSTM 与序列','同样的元素，顺序可能改变意义。',['dl-backprop'],[
 ['状态把过去带到下一步','RNN 在时刻 t 用输入 x_t 和先前隐藏状态 h_(t−1) 生成新状态。参数跨时间共享，反向传播沿时间展开。很长的依赖会面临梯度消失或爆炸。'],
 ['门控制保留与遗忘','LSTM 用门控机制管理细胞状态中的信息流，缓解长程依赖困难。门并不是具有人类意图的开关，而是可学习的数值函数。处理批次时要注意序列长度、padding 和 mask。']
],`import math
h = 0.0
for x in [1, 0, -1]:
    h = math.tanh(0.5*h+x)
    print(round(h,4))`,'每一步依赖当前输入和上一状态；交换输入顺序，最终状态一般改变。',['训练样本之间是否继承隐藏状态，需要明确设计。','填充位置参与损失可能教会模型无意义模式。'],[
 Q('RNN 在时间步之间共享什么？',['模型参数','每步所有输入必须相同','每步标签必须相同'],0,['正确，状态改变而参数复用。','输入通常不同。','标签不必相同。']),
 Q('LSTM 的门主要帮助？',['控制信息保留与流动','保证永不遗忘','自动获得监督标签'],0,['正确，为长程信息提供更可控路径。','没有这种绝对保证。','门不产生真实标签。'])
],['d2l'],'比较同一个短序列正序与倒序的状态，解释顺序如何进入计算。',{depth:'算法进阶'}),
L('dl-attention','Attention 与 Transformer','让当前位置选择该看哪些信息。',['dl-tensors','math-info','dl-sequence'],[
 ['把 Q、K、V 当作三个角色','Query 表示当前位置的查询；Key 用于匹配；Value 是被汇聚的信息。缩放点积注意力计算 softmax(QKᵀ/√d_k)V。权重是动态的，不是每次固定取邻居。'],
 ['一行一行算','先算每个 key 与 query 的分数；缩放后用 softmax 归一化；用这些权重加权 values。多头在不同投影空间重复此过程。位置编码补充次序信息，因果 mask 防止预测时看到未来。'],
 ['完整结构还有别的责任','Transformer 块通常包含注意力、前馈网络、残差和归一化。注意力图可以帮助观察，但不能直接当成模型决策的完整因果解释。']
],`import math
scores = [1.0, 2.0, 0.0]
values = [10.0, 20.0, 30.0]
m = max(scores)
exps = [math.exp(s-m) for s in scores]
weights = [e/sum(exps) for e in exps]
print([round(w,4) for w in weights])
print(round(sum(w*v for w,v in zip(weights,values)),4))`,'权重约 [0.2447,0.6652,0.09]，输出约 18.4530。分数最高项贡献最大，但其他项仍参与。',['softmax 前减去最大值改善数值稳定性，不改变数学结果。','没有位置机制的纯自注意力不自然知道序列顺序。'],[
 Q('因果 mask 在自回归训练中主要防止？',['当前位置看未来 token','模型读取过去','模型做矩阵乘法'],0,['正确，保持训练条件与生成条件一致。','过去通常允许读取。','矩阵乘法仍是核心计算。']),
 Q('softmax 后的一行权重通常满足？',['非负且和为 1','每个都等于 1','可以任意大且不归一化'],0,['正确，形成加权汇聚。','只有特殊单元素场景才如此。','这不是 softmax 的结果。'])
],['attn'],'手算一个 2-token 注意力矩阵，再说明 mask 在哪一步加入。',{depth:'算法进阶',minutes:60}),
L('dl-selfsupervised','自监督与对比学习','标签可以来自数据变换，但目标仍需设计。',['dl-attention','ml-unsupervised'],[
 ['从数据构造学习信号','遮盖词预测、下一 token 预测、图像不同增强视图匹配，都是自监督目标。它们减少人工标注依赖，但不会消除数据质量、版权或分布问题。'],
 ['对比学习的取舍','让同一对象的两种合理视图接近，同时与其他对象区分。增强定义了什么变化应被忽略；如果颜色本身决定标签，随意丢弃颜色就可能损害任务。表示好坏最终由下游评估检验。']
],`import math
def cosine(a,b):
    return sum(x*y for x,y in zip(a,b)) / (math.sqrt(sum(x*x for x in a))*math.sqrt(sum(y*y for y in b)))
print(round(cosine([1,0],[0.9,0.1]),4))
print(cosine([1,0],[0,1]))`,'第一对相似度约 0.9939，正交向量为 0。零向量在该定义下需要额外处理。',['相似度高不等于事实相同。','错误的正样本或假负样本会影响训练信号。'],[
 Q('对比学习的数据增强实际上定义了什么？',['希望表示保持不变的变化','所有数据的真实标签','必定最优的网络层数'],0,['正确，因此必须结合任务选择。','增强不自动提供语义真相。','架构还需设计与评估。']),
 Q('自监督表示好坏最终应如何判断？',['看下游任务与独立评估','只看向量维度大不大','看名字是否含 AI'],0,['正确，训练目标只是代理。','更高维不保证更有用。','名称不是证据。'])
],['simclr','llm'],'为一个业务数据集设计两种合理增强，并解释一种不合理增强。',{depth:'算法进阶',minutes:45}),
L('dl-generative','VAE、GAN 与扩散模型','三种生成方式，三种训练信号。',['dl-backprop','math-info','math-prob'],[
 ['先区分目标','VAE 学潜变量表示，用重建项与 KL 等约束优化变分下界；GAN 让生成器和判别器进行对抗优化；扩散模型通过噪声过程和去噪学习构造生成过程。它们不是同一个损失换了名字。'],
 ['从加噪直觉进入扩散','常见前向过程写成 x_t=√ᾱ_t x_0+√(1−ᾱ_t)ε。随着保留信号减少，输入逐渐由噪声主导；模型学习条件去噪或等价参数化。完整训练和采样需进一步学习噪声日程、目标和数值方法。']
],`import math, random
random.seed(7)
x0 = 1.0
noise = random.gauss(0,1)
for alpha_bar in [0.9, 0.5, 0.1]:
    xt = math.sqrt(alpha_bar)*x0 + math.sqrt(1-alpha_bar)*noise
    print(alpha_bar, round(xt,4))`,'使用同一个噪声样本观察信号权重变化；这只是前向加噪，不是完整生成模型。',['生成样本好看不证明分布覆盖良好。','这一关是算法导览；训练级掌握需要按原文复现和独立评估。'],[
 Q('GAN 的核心训练关系是？',['生成器与判别器的对抗目标','只有固定最近邻','只做 SQL 聚合'],0,['正确，训练稳定性也是其挑战。','不是 GAN 定义。','与生成训练不同。']),
 Q('扩散前向公式中 ᾱ_t 越小，原信号系数通常？',['越小','越大','恒为 1'],0,['正确，√ᾱ_t 减小。','公式方向相反。','它随日程变化。'])
],['ddpm','d2l'],'选择一种生成范式，整理训练目标、采样过程、计算代价与失败模式。',{depth:'算法导览',minutes:60}),
];
