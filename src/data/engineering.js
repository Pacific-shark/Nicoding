import {lesson as L,question as Q} from './schema.js';
export const engineering = [
L('eng-git','Git 与差异阅读',"区分工作区、暂存区和提交，检查实际改动。",['py-functions'],[
 ["三处位置", "工作区是你编辑的文件；暂存区是下一次提交准备包含的内容；提交是可追踪的快照。git diff 看未暂存差异，git diff --staged 看将提交的差异。"],
 ["审阅 AI 修改的最小单位", "先看文件范围，再看函数输入输出，再检查异常路径和测试。一次提交围绕一个可说明的意图；不要只看“运行成功”就接收几千行改动。删除和密钥意外入库尤其要看清。"],
 ["一次修改怎么进入提交", "修改 app.py 后，改动还在工作区。执行 git add app.py 才把当时的文件内容放进暂存区。之后若又编辑 app.py，新修改不会自动进入已暂存的版本。\n\n所以提交前要分别看 git diff 和 git diff --staged：前者检查还有哪些未暂存改动，后者检查本次究竟准备提交什么。commit 保存到本地历史；push 才把提交发到远端。"]
],`before = {"timeout": 3, "retries": 0}
after = {"timeout": 3, "retries": 2}
for key in before.keys() | after.keys():
    if before.get(key) != after.get(key):
        print(key, before.get(key), "->", after.get(key))`,'输出 retries 0 -> 2。这是 Python 差异类比；真正的 Git 命令在终端运行。',['git add 不等于已经提交；commit 不等于已经推送。','不理解历史改写时，不要用强制推送作为默认修复。'],[
 Q('想检查下一次提交包含什么，先看？',['git diff --staged','只看页面截图','清空全部文件'],0,['正确，它比较暂存区与当前提交。','看不到非视觉改动。','这是破坏，不是检查。']),
 Q('AI 修改 12 个文件，最先确认什么？',['文件范围是否符合任务','代码行数越多越好','直接全部接受'],0,['正确，范围是审阅入口。','行数不是质量指标。','会遗漏不相关修改。'])
],['git'],'读一次真实 diff：每个文件用一句话解释修改意图，找出无法解释的部分。',{priority:1}),
L('eng-contract','模块边界与接口契约',"明确输入、输出、单位、错误和各层职责。",['py-functions','py-json'],[
 ["从数据流切职责", "以优惠计算为例：接口层解析请求；校验层检查输入；计算层返回金额；存储层持久化；展示层解释结果。边界不是文件越多越好，而是依赖方向和责任清楚。"],
 ["写出能被反驳的约定", "契约至少含输入形状、输出形状、错误、权限、副作用。说明折扣率用 0.1 还是 10，金额用元还是分；这些小歧义会造成真错误。接受条件用具体例子表达。"],
 ["用一个输入发现单位歧义", "amount=100、discount=10 还不足以判断结果：100 是元还是分？10 表示减 10%、付 10%，还是直接减 10 元？\n\n先约定输入 amount_cents=10000、discount_percent=10，输出 final_cents=9000。这样接口、计算函数和测试都有同一个可核对的例子。划分模块时再问：谁解析输入，谁计算金额，谁写入订单。"]
],`def quote_cents(price_cents, rate_percent):
    if price_cents < 0 or not 0 <= rate_percent <= 100:
        raise ValueError("输入越界")
    return round(price_cents * (100 - rate_percent) / 100)

print(quote_cents(20000, 10))`,'返回 18000 分。示例使用 Python round 的舍入规则，生产业务需明确舍入政策。',['同名字段若单位不同，就是不同契约。','“负责所有东西”的模块通常很难独立测试。'],[
 Q('接口 amount=100 仍缺少什么关键约定？',['单位和范围','字段颜色','文件图标'],0,['正确，100 元与 100 分差一百倍。','显示风格不定义数值含义。','与契约无关。']),
 Q('纯计算函数最适合承担哪项？',['计算输入到输出的规则','同时发邮件、扣库存、改页面','自动决定所有权限'],0,['正确，副作用可在外层编排。','责任耦合后更难验证和复用。','权限需要单独设计。'])
],['py','http'],'把当前功能画成输入→校验→计算→存储→展示，为每段写负责人和错误出口。',{priority:1}),
L('eng-debug','调试证据与 AI 协作',"用可复现的错误缩小排查与修改范围。",['py-errors','eng-git'],[
 ["上下文应围绕问题闭合", "交给 AI：目标、当前行为、最小复现、相关调用链、允许修改范围、通过标准。省 token 的重点是减少猜测和反复改错；关键错误栈不能为了短而删掉。"],
 ["审查它的假设", "让 AI 区分观察和推测，先说明哪条证据支持根因。一次修改一个原因，再运行能覆盖原失败的验证。无需把整个仓库每次都贴入，但要确保给出的局部片段保留上下游契约。"],
 ["把猜测变成一次检查", "界面收到库存 0 却显示 100。若发现代码是 stock || 100，可以直接运行 0 || 100，验证零被默认值替换的假设；再用 null 和正常库存对照。\n\n给 AI 的材料只需要覆盖这条输入路径和显示逻辑。如果它建议重写数据库，应追问这与已经复现的前端判断有什么关系。"]
],`report = {
    "input": {"amount": 0},
    "expected": 0,
    "actual": 100,
    "hypothesis": "默认值逻辑把零当作缺失"
}
print(report["expected"] == report["actual"])
print(report["hypothesis"])`,'False 说明观察到不一致；假设还需定位代码才能证实。',['更短的提示词不一定降低总成本，返工也是成本。','测试通过是范围内的证据，不是“绝对没有 Bug”。'],[
 Q('希望减少 AI 返工，最该补充什么？',['只说“高级一点”','失败输入和可验证的预期','要求它自信回答'],1,['缺少可检查的条件。','正确，模型能据此收敛修改。','自信不能替代证据。']),
 Q('“可能是缓存问题”属于？',['已证明事实','待验证假设','测试结果'],1,['需要排除或验证。','正确，下一步设计能区分假设的观察。','没有运行验证便没有结果。'])
],['err','git'],'用六行写一个给 AI 的任务包：目标、范围、输入、预期、错误、验证命令。',{priority:1}),
L('eng-tests','测试与回归',"从规则写出预期，用回归用例保护修复。",['eng-contract','eng-debug'],[
 ["从行为推导测试", "正常输入、边界、空输入、错误输入是四个入口。测试期望来自契约，不应只是照抄实现。discount(200,0.1)=180 能检验一个案例，还应测试零折扣和全额折扣。"],
 ["分层验证", "单元测试验证小逻辑，集成测试验证模块之间，端到端测试验证用户路径。外部网络可在适当层替换成可控响应，但至少要有真实联通证据。失败案例修复后留下回归测试。"],
 ["一条断言为什么能拦住错误", "满 100 包邮，金额 99、100、101 的运费应分别为 8、0、0。把条件从 >= 误写成 > 时，99 和 101 仍能通过，只有恰好 100 的例子能发现错误。\n\n因此先从业务规则独立写预期值，再运行实现。若测试里的预期结果也调用同一个运费函数计算，即使函数写错，两边也会一起错。"]
],`def shipping(amount):
    return 0 if amount >= 100 else 8

cases = [(99, 8), (100, 0), (101, 0)]
for value, expected in cases:
    assert shipping(value) == expected, (value, expected)
print("3 个边界案例通过")`,'三例通过；若把 >= 改成 >，100 这一例会失败。',['覆盖率高不代表断言有意义。','把期望值也用被测函数算出来，会失去独立判断。'],[
 Q('满 100 包邮，最有区分力的一组输入？',['只有 200','99、100、101','全部随机长文本'],1,['看不到边界是否写错。','正确，紧贴阈值能检出 > 与 >= 的差异。','与金额契约不匹配。']),
 Q('修复线上错误后，优先补哪种测试？',['能复现原错误的回归用例','只测不相关页面','只增加注释'],0,['正确，防止相同问题悄悄回来。','不能保护本次修复。','注释不会执行验证。'])
],['test'],'给你负责的一项业务规则补三条断言，并证明至少一条能拦住旧错误。',{priority:1}),
L('eng-sql','SQL、连接与空值',"确定数据粒度，检查连接和 NULL 的影响。",['py-containers'],[
 ["先确定粒度，再做连接", "订单表一行一单，明细表一行一件；JOIN 后订单金额可能被复制多次。聚合前判断目标是一人、一单还是一件。主键与唯一性约束帮助表达粒度。"],
 ["NULL 不等于零", "SQL 的 NULL 表示缺失，判断用 IS NULL。COUNT(*) 统计行，COUNT(column) 忽略该列 NULL。LEFT JOIN 保留左表，但在 WHERE 中过滤右表可能排除没匹配的行。"],
 ["JOIN 后先数行，再求和", "订单 A 的金额是 100，有两条商品明细；订单 B 的金额是 50，有一条明细。连接后出现 A、A、B 三行。此时对订单金额求和得到 250，实际总额却是 150。\n\n如果只需要订单总额，就在订单粒度求和。需要明细统计时，可先把明细按订单汇总，再和订单表连接。用 SUM(DISTINCT amount) 不能通用地修复：两张不同订单也可能恰好同价。"]
],`import sqlite3
db = sqlite3.connect(":memory:")
db.execute("CREATE TABLE orders (id INTEGER, amount REAL)")
db.executemany("INSERT INTO orders VALUES (?, ?)", [(1, 20), (2, None), (3, 0)])
print(db.execute("SELECT COUNT(*), COUNT(amount), SUM(amount) FROM orders").fetchone())
db.close()`,'输出 (3, 2, 20.0)。这里用 SQLite 演示通用语义；生产需核对 PostgreSQL 等方言。',['SUM 不会自动把所有缺失业务解释为零；缺失原因仍需追踪。','拼接用户输入构造 SQL 容易出错，应参数化查询。'],[
 Q('订单连接多条明细后，直接 SUM(订单金额) 可能？',['重复统计金额','永远正确','自动去重'],0,['正确，一对多连接扩张了行数。','必须先检查粒度。','SQL 不会替你理解业务唯一性。']),
 Q('COUNT(amount) 对 NULL 如何处理？',['不计入该列计数','按零计入','整条查询报错'],0,['正确，与 COUNT(*) 区别在这里。','零会计入，NULL 不会。','这是合法聚合。'])
],['sql'],'为一个业务报表标注原表、连接后表、最终结果各自的行粒度。',{priority:1}),
L('eng-transaction','事务、幂等与重试',"处理重复请求，以及执行结果未知的情况。",['eng-sql','web-http'],[
 ["事务保护一组变化", "扣余额与增加订单要么共同成功，要么按设计回滚。隔离级别决定并发观察，不能把“用了事务”当作所有竞争问题都解决了。"],
 ["区分没收到和没执行", "客户端超时可能发生在服务端已完成之后。重试付款必须使用稳定幂等键和服务端唯一约束等机制；本地加一个布尔变量挡不住多实例并发。下面字典只是单进程类比。"],
 ["收到超时后的一条安全路径", "请求已经在服务端创建订单，但响应返回途中丢失，客户端只能看到超时。此时“没有收到成功”不能推出“没有执行”。\n\n重试应沿用同一幂等键。服务端原子地检查或创建记录，重复请求返回已有结果；相同键却带不同金额，应按契约拒绝或报冲突。单进程字典可以帮助理解流程，无法替代多实例下的持久约束。"]
],`seen = {}
def create_order(key, amount):
    if key not in seen:
        seen[key] = {"id": len(seen) + 1, "amount": amount}
    return seen[key]
print(create_order("req-1", 20))
print(create_order("req-1", 20))
print(len(seen))`,'重复同一键仍只有一笔；实际系统还要校验相同键的请求内容一致。',['GET 通常应无业务写副作用，但方法名本身不保证实现正确。','指数退避和抖动缓解重试冲击，不会自动实现幂等。'],[
 Q('客户端支付超时，能断定付款未执行吗？',['能','不能，需要查询状态或安全重试','重复支付一定免费'],1,['响应可能在返回途中丢失。','正确，要处理结果未知。','没有这样的保证。']),
 Q('多实例服务防止重复创建，重要机制是？',['每个进程各设一个 bool','持久幂等键与原子唯一约束','把按钮变灰就够了'],1,['实例之间不共享这个变量。','正确，最终约束要靠服务端可靠存储。','请求仍可能来自其他路径。'])
],['sql','http'],'给一个写接口设计幂等键、超时查询路径和最大重试次数。'),
L('eng-delivery','配置、权限与部署',"检查运行配置、服务端权限和回滚方法。",['eng-tests','web-http'],[
 ["区分构建与运行配置", "前端构建时注入的变量通常会成为公开资源；后端机密从受控运行环境读取。示例配置只列键和说明，不提交真实值。发布要有版本、启动命令、健康检查和回滚方案。"],
 ["权限在可信边界判断", "隐藏按钮只是体验，服务端还必须验证当前用户能否操作该资源。失败日志帮助定位，但避免记录令牌和隐私原文。上线前检查空状态、错误状态和恢复路径。"],
 ["隐藏按钮不能保护订单", "把“修改订单”按钮隐藏后，使用者仍可能直接发请求。服务端收到订单 ID 时，需要从已验证的登录状态取得用户身份，再检查这张订单是否属于他。\n\n请求正文中自带的 user_id 或 role 不能直接作为授权依据。代码能在本地运行，也不代表线上配置、数据库权限和失败恢复已经验证。"]
],`def can_edit(user_id, owner_id, role):
    return user_id == owner_id or role == "admin"

print(can_edit("nico", "milo", "member"))
print(can_edit("nico", "nico", "member"))`,'结果 False、True。实际系统的 user_id/role 必须来自已验证身份。',['不能相信请求正文中自称的 admin 角色。','只保留最新部署而无回滚策略，会让故障恢复困难。'],[
 Q('用户把请求里的 role 改成 admin，服务端应该？',['直接相信','使用已验证身份与权限规则','仅检查按钮是否显示'],1,['客户端数据不可信。','正确，授权必须在可信边界执行。','按钮无法保护接口。']),
 Q('发布前最应准备什么？',['版本、健康检查、回滚路径','只改一个漂亮名称','删除所有日志'],0,['正确，交付包括可观察与恢复。','不能替代运行保障。','会丢失定位证据。'])
],['http','venv'],'写一页交付说明：如何启动、如何判断正常、出错看哪里、怎样回滚。'),
L('eng-product','需求、验收与技术取舍',"把用户需求写成可检查的行为和取舍。",['eng-contract'],[
 ["从用户任务开始", "“加一个 Agent”没有说明用户为什么需要它。先定义用户、场景、目前阻碍与成功标准。例如把人工整理报告从 30 分钟降到 10 分钟，同时保留每个事实的来源。"],
 ["管理约束与取舍", "延迟、成本、正确率、隐私、可维护性不会自动同时最优。用最小实验验证最不确定的假设，把必要功能、后续功能和不做的范围写清。自动测验只能检验局部能力，项目评审仍要看交付证据。"],
 ["给验收写出输入和结果", "“保存进度”可以具体到：完成一节课后刷新页面，仍显示已完成；导入坏格式文件时提示错误，当前记录保留；换设备是否同步则是另一项需要账号和后端的需求。\n\n把这些场景提前列出来，开发者才能判断做完了什么。成功率、耗时等指标也要写明样本来源与测量方式，不能只写“体验更好”。"]
],`baseline_minutes = 30
after_minutes = [9, 12, 8, 11]
mean = sum(after_minutes) / len(after_minutes)
print("样本平均分钟:", mean)
print("相对节省:", (baseline_minutes - mean) / baseline_minutes)`,'四次样本平均 10 分钟；样本很少，还不能推断所有用户都节省相同比例。',['功能数量不等于用户价值。','验收指标需要说明测量方式、样本和失败条件。'],[
 Q('“做得智能一点”最缺什么？',['具体任务和可验收结果','更多缩写','更大的模型名字'],0,['正确，应转为可以观察的用户行为。','缩写不提供验收标准。','模型只是可能的手段。']),
 Q('四个试用样本效果好，适合怎样报告？',['保证所有用户同样受益','报告样本与限制，继续验证','删除不好的样本'],1,['超出了样本证据。','正确，保留不确定性才能做可靠决策。','会引入选择偏差。'])
],['test','stats'],'为自己的项目写用户任务、成功指标、范围、验收例子和最大未知项。',{priority:1}),
];
