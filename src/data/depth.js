// Original worked reasoning and transfer prompts. Reference readings live on each lesson.
const D=(plain,steps,question,answer,teach,checks)=>({plain,steps,question,answer,teach,checks});
export const depth={
 'py-values':D('变量名像贴在对象上的标签。赋值改变标签指向，不能据此推断对象被复制；值相等也不代表是同一个对象。',[
 'a = [1]；b = a：两个名字指向同一个列表。','b.append(2)：原列表变为 [1, 2]，所以 a 也看得到。','b = [9]：这次只把 b 重新绑定，a 仍是 [1, 2]。'],
 'a = [1]，b = [1] 时，== 和 is 各判断什么？为什么不该用小整数实验推导 is 的普遍规则？','== 比较值，两个列表相等；is 比较身份，这里为 False。解释器可能复用某些不可变对象，但这种现象不是业务值比较的契约。','给 Nico 解释：为什么一次赋值没有制造一份新列表？',['区分名字、对象、值','用修改与重新赋值各举一例','说明值比较应使用 ==']),
 'py-flow':D('控制流就是程序下一步走向哪里。读循环时记录每轮状态，比只看最后一个 print 更可靠。',[
 'total = 0；依次处理 [2, 0, 3]。','遇到 0 执行 continue，只跳过本轮；total 依次为 2、2、5。','若把 continue 换成 break，循环在 0 处结束，total 为 2。'],
 '为什么 range(3) 不包含 3？如何遍历相邻项且不越界？','range 采用右端不含的区间，产生 0、1、2。相邻项可用 zip(items, items[1:])；用下标则循环到 len(items)-2，空序列也应安全。','不用“就是这样规定”的回答，手动讲一次循环何时进入、跳过、停止。',['逐轮写出状态','区分 continue 与 break','说明空输入与末尾边界']),
 'py-functions':D('函数把一条规则装进可重复调用的单元。参数是本次输入，return 是交回调用者的结果，print 只是显示。',[
 'def net(price, rate): return price * (1-rate)。','调用 net(100, 0.2)：局部名字 price=100、rate=0.2，返回 80。','如果只有 print(80) 而没有 return，调用者拿到 None，不能把屏幕显示当返回值。'],
 'def add(x, bucket=[]) 为什么多次调用会积累数据？如何保持每次默认独立？','默认参数表达式在定义函数时求值，那个列表会被复用。改用 bucket=None，再在函数内用 if bucket is None: bucket=[]；不要用 if not bucket 覆盖调用者传入的空列表。','向刚会调用函数的人解释参数、返回值和可变默认参数的陷阱。',['跟踪一次实际调用','解释 print 与 return','用 None 方案解释默认值生命周期']),
 'py-containers':D('容器决定数据如何组织和查找。列表保留序列，字典按键关联，集合关注去重与成员关系；选择取决于操作。',[
 '订单列表含两条 sku="A" 的记录，数量分别为 2、3。','遍历时 totals[sku] = totals.get(sku, 0) + qty，得到 {"A": 5}。','直接写 totals[sku] = qty 会覆盖前一条，只剩 3。'],
 '什么时候不能用列表下标代替业务 ID？字典查找为什么通常快，但不能说永远 O(1)？','位置会因排序、插入、筛选改变；业务身份应独立保存。字典通常基于哈希，平均查找为常数时间，但碰撞、哈希计算和最坏情况使“永远”不成立。','解释为什么订单聚合适合字典，并说明重复键会发生什么。',['按操作选择容器','手算重复键聚合','区分身份与位置']),
 'py-references':D('浅拷贝只新建最外层，里面的对象仍可能共享。判断隔离性要沿着引用走到实际修改的位置。',[
 'a = [[1], [2]]；b = a.copy()：外层不同，内部列表共享。','b[0].append(9)：a[0] 也变成 [1, 9]。','b[0] = [7]：只替换 b 的一个引用，不再修改 a[0]。'],
 'rows = [[0]*3]*2 为什么改一个格子可能两行都变？','乘法重复的是同一内层列表的引用。用 [[0]*3 for _ in range(2)] 才会逐次创建内层列表；复杂对象还需明确浅拷贝、深拷贝或不可变结构的语义。','画两个箭头，解释浅拷贝后哪一层独立、哪一层共享。',['标出共享内层','区分重绑与原地修改','给出独立建行的写法']),
 'py-errors':D('异常携带失败的位置和原因。捕获异常应改变失败处理方式，而不是让错误看起来像正常结果。',[
 'int("12x") 抛出 ValueError；访问缺失字典键可能抛出 KeyError。','在解析边界捕获 ValueError，返回明确的输入错误或继续抛出带上下文的异常。','如果 except Exception: return 0，就把坏输入伪装成了合法的零。'],
 '清理资源应该依赖 except 还是 finally？with 的作用是什么？','finally 在离开受保护代码时执行清理；with 通过上下文管理器安排进入和退出。它们不等于吞掉异常；是否抑制异常由具体处理逻辑决定。','向同事解释为何“没报错”不等于程序正确。',['区分预期输入错误与程序缺陷','保留可定位的上下文','说明资源清理与异常传播']),
 'py-modules':D('模块是代码组织单位，运行环境决定导入哪个实现。安装成功与当前解释器能导入不是同一件事。',[
 '两个虚拟环境都有 python，但各自的包目录可以不同。','python -m pip 将安装命令绑定到这个 python，减少 pip 与解释器错位。','用 sys.executable、模块的 __file__ 核对实际执行位置。'],
 '为什么项目文件命名为 json.py 可能破坏 import json？入口保护解决什么问题？','导入搜索可能先找到本地同名文件，遮蔽标准库。if __name__ == "__main__" 让演示入口只在直接执行时运行；它不会自动解决循环依赖或错误命名。','解释同一份代码“我这里能跑，你那里不能”的一个可核验原因。',['明确解释器与包环境','核对模块来源','区分导入副作用与入口执行']),
 'py-json':D('JSON 是数据交换格式，不是可信业务对象。解析成功只说明语法能读，仍要检查字段、类型和范围。',[
 'json.loads 将 {"qty":2} 读成 Python 字典。','先要求最外层是字典，再检查 qty 存在且是整数。','bool 是 int 的子类；若数量不允许布尔值，可用 type(qty) is int，最后检查 qty > 0。'],
 '为什么不能先计算再捕获所有异常来代替输入校验？','合法运算也能产生业务错误，例如负数量计算出负金额。结构、类型、范围属于契约，应在副作用前验证；未知字段是否拒绝也要明确。','用一份语法正确但业务非法的 JSON 解释两层校验。',['区分解析与校验','覆盖缺失、错误类型、范围','在计算或写入前拒绝坏输入']),
 'web-types':D('JavaScript 的隐式转换会把“看起来相似”的值接到不同规则上。业务判断应先决定哪些值代表缺失。',[
 '0 || 10 得到 10，因为 0 为假值。','0 ?? 10 得到 0，因为 ?? 只处理 null 与 undefined。','"5" + 1 是 "51"，"5" - 1 是 4；不要依赖操作符替你猜业务类型。'],
 'Number("") 得到 0，为什么表单只写 Number(input) 不够？','转换规则不等于输入有效性。先明确空白是否允许，再解析，并用 Number.isFinite 检查有限数；需要整数时还要检查 Number.isInteger。','向 Nico 解释库存为 0 时，为何默认值可能让界面显示错。',['区分假值与缺失','给出 || 与 ?? 的反例','说明显式校验仍必要']),
 'web-scope':D('闭包让函数保留对外部词法作用域的访问。它保存的不是每个变量某一瞬间的照片。',[
 '外部 let count=0，函数 next 执行 count += 1 并返回 count。','连续调用两次得到 1、2，因为访问同一个绑定。','每调用一次创建计数器的外层函数，会创建一组新的局部绑定。'],
 '循环中用 var 和 let 注册稍后执行的回调，为什么可能得到不同序号？','var 是函数作用域，回调可能共同读取循环结束后的绑定；let 的循环头为每轮建立绑定。关键是回调执行时间和它捕获的绑定，而不是回调自带错误。','解释两个计数器为何既能分别累加，又互不干扰。',['说清词法作用域','区分绑定与值快照','结合执行时间解释回调']),
 'web-objects':D('展开语法只复制一层属性。界面状态更新既要保留旧状态，也要确保真正变化的路径有新引用。',[
 'old={user:{name:"Nico"},done:false}。','next={...old} 后修改 next.user.name，仍会改到 old.user。','next={...old,user:{...old.user,name:"Milo"}} 才对这条路径建立新对象。'],
 '为何 JSON.parse(JSON.stringify(obj)) 不是通用深拷贝？','JSON 无法完整保留所有 JS 值和对象语义；undefined、日期、循环引用等会丢失、转换或报错。可用 structuredClone 处理其支持的类型，但仍应先确定要复制哪些数据。','不用“深浅”两个字，说明一次嵌套对象更新改了哪几层。',['标出共享引用','仅复制变化路径','指出 JSON 复制的边界']),
 'web-dom':D('DOM 表达文档结构，事件表达用户动作。可访问性语义让同一个动作也能被键盘和辅助技术理解。',[
 'button 默认可获得焦点并支持键盘激活。','给父容器注册 click，可从 event.target 找到点击来源，currentTarget 是注册监听的元素。','用 closest 找目标时，要确认它属于当前容器，避免处理到无关元素。'],
 '为什么用可点击 div 模拟按钮通常会多出工作？','需要自行补键盘行为、焦点、角色、禁用语义等；原生 button 已有适合动作的基础行为。导航用 a，动作按钮用 button，语义应符合用户目标。','描述鼠标点击和键盘 Enter 怎样触发同一个功能。',['区分结构与事件','区分 target 与 currentTarget','选择匹配行为的原生元素']),
 'web-async':D('await 暂停的是当前异步函数的后续部分，不会冻结整个页面。读异步代码要画出依赖与完成顺序。',[
 '先同步打印 A，安排 Promise 回调打印 B，再同步打印 C。','同步部分先完成，输出 A、C；随后微任务输出 B。','两个请求没有依赖时可一起启动再 await Promise.all，有依赖时必须按先后传值。'],
 '搜索先发 A 后发 B，A 更晚返回，为什么页面会倒退？','网络完成顺序不保证与发起顺序一致。使用请求序号忽略旧响应，或取消旧请求并正确处理取消；单纯 await 各自请求不解决跨请求竞争。','向产品同事解释加载状态和旧请求覆盖问题。',['区分调用与完成顺序','说明并发何时安全','给出过期响应处理策略']),
 'web-http':D('HTTP 把一次请求拆成方法、地址、头与正文；响应还包含状态。网络通了不代表业务成功。',[
 'fetch 收到 404 通常仍会返回 Response，而非自动抛出网络异常。','先检查 response.ok 或明确状态，再按约定解析正文。','401、403、429 的后续处理不同，错误状态要保留可识别信息。'],
 '为什么付款请求超时后直接重试可能重复扣款？','超时只表示调用方没有及时拿到结果，服务器可能已经执行。需要服务端幂等键和结果查询等机制；客户端禁用按钮不能覆盖网络重试与多端请求。','讲清一次点击发出请求后，可能在哪三层失败。',['区分网络、HTTP、业务错误','说明状态与正文检查','解释超时结果的不确定性']),
 'web-typescript':D('TypeScript 在开发阶段检查类型关系。网络返回的数据在运行时仍可能违反声明，类型断言不会替你验证。',[
 '把 JSON 强制断言为 User，只改变编译器的看法。','先把外部值视为 unknown，检查对象、字段和类型。','校验函数成功后再进入业务逻辑，失败则展示明确错误。'],
 '联合类型 loading | success | error 如何减少不可能的 UI 状态？','把每个状态及其数据绑定，例如 success 才含 data，error 才含 message。相比多个独立布尔值，可减少同时 loading 和 error 等矛盾；仍要正确实现状态转换。','解释“类型检查全绿，线上仍报错”如何发生。',['区分静态类型与运行时值','说明 unknown 的检查路径','用判别联合表达状态']),
 'web-react':D('React 根据状态渲染界面。一次渲染里的值是那次渲染的快照，状态更新用于请求新的渲染。',[
 'count=0 时连续 setCount(count+1) 两次，两个调用都请求设为 1。','用 setCount(c=>c+1) 两次，更新函数依次计算，结果为 2。','列表项的 key 用稳定业务 ID，帮助更新时维持正确身份。'],
 '为什么能从现有状态算出的 filteredItems 通常不必再存一份 state？','双份来源需要同步，容易出现陈旧数据。渲染时派生即可，昂贵计算再考虑缓存；副作用用于与外部系统同步，而不是机械复制内部状态。','讲清一次点击、状态更新、重新渲染的因果链。',['说明渲染快照','用函数式更新处理连续变化','区分派生数据与独立状态']),
 'eng-git':D('提交是一个可描述、可核验的改动单元。读 diff 的目的，是确认改动范围与需求一致。',[
 '先看 git status 分清修改、暂存与未跟踪文件。','git diff 看未暂存变化，git diff --staged 看将提交的内容。','只在验证后提交，把原因和检查结果写进记录，便于之后定位回归。'],
 '回滚已共享的提交时，revert 与 reset 有什么重要区别？','revert 新增一个反向修改提交，保留共享历史。reset 移动分支位置，并可能改变索引或工作区；对共享分支重写历史会影响其他协作者，应依据具体场景选择。','假设 AI 改了十个文件，你如何判断哪些应该进这次提交？',['检查暂存与未暂存差异','把改动映射到需求','区分新增回退与历史重写']),
 'eng-contract':D('契约回答输入是什么、输出是什么、失败怎样表达、谁负责验证。明确边界能降低 AI 自行猜测的空间。',[
 '优惠接口接收金额分整数和折扣整数百分比。','成功返回金额分；缺字段、负数、非整数按约定返回输入错误。','解析、计算、存储各有职责，计算函数不应顺手发送请求或改数据库。'],
 '接口加一个字段为什么也可能破坏兼容性？','调用者可能严格校验字段、依赖枚举穷尽或序列化形式。兼容性取决于双方契约；新增必填字段、改变单位或空值语义尤其容易破坏旧调用。','用一条订单规则写出双方都不必猜的输入与错误约定。',['给出具体单位与类型','说明错误与副作用','考虑旧调用方的行为']),
 'eng-product':D('需求需要从形容词变成可观察的行为。“更好用”要落到具体用户、任务、状态和验收条件。',[
 '用户任务：零库存商品要明确提示不能购买。','正常例库存 1 可购买；边界例库存 0 显示售罄；缺失库存显示待确认。','验收不仅看文案，也看按钮状态、键盘操作和刷新后是否一致。'],
 '完成率提高就能断定改版有效吗？','还要检查用户构成、观察窗口、样本量、埋点变更与护栏指标。先定义目标和比较方法，避免看到结果后换指标；行为变化也可能有外部原因。','把“做一个智能助手”改写为一个可验收的小需求。',['明确用户与触发条件','覆盖正常和异常状态','将指标与具体任务对应']),
 'eng-debug':D('调试是用证据排除假设。一次改很多处虽可能碰巧恢复，也会让你不知道原因。',[
 '库存 0 显示默认值：先构造输入 0 的最小失败例。','对照输入 1、null，怀疑假值回退把 0 当成缺失。','只改相关判断，再跑三个例子，检查其他合法假值是否受影响。'],
 '错误栈最顶层就是根因吗？','错误栈说明异常传播路径，出错点可能只是收到坏输入的地方。沿数据来源查明哪个边界首次违反契约，用最小复现和对照实验验证。','讲一次从现象到假设、实验、修复的完整过程。',['保留可重复的失败例','每次控制改动范围','用回归检查验证因果假设']),
 'eng-tests':D('测试把“应该怎样”变成可重复的检查。好测试针对行为与边界，不能只是把实现再写一遍。',[
 '优惠计算：正常输入 20000 分、10%，期望 18000 分。','加上 0%、100%、负数和非法类型，检查边界与错误路径。','再检查原输入未被修改；正确数值不代表所有副作用都正确。'],
 '100% 行覆盖为什么不能证明没有 bug？','覆盖只说明代码被执行，没有说明断言足够、输入空间充分或业务期望正确。可用变异、边界、性质测试补充；外部接口还需契约与集成检查。','解释一个“能跑过，却没测到问题”的测试。',['断言来自需求','覆盖边界与副作用','说明有限样本的局限']),
 'eng-sql':D('写 SQL 前先决定一行表示什么。连接会改变行数，聚合指标必须对准业务粒度。',[
 '一张订单表每单一行，明细表一单三行。','连接后该订单金额出现三次；直接 SUM(order.amount) 会重复统计。','先按订单聚合明细，或在正确粒度计算指标，再做必要连接。'],
 'LEFT JOIN 后在 WHERE 里要求右表字段等于某值，会发生什么？','未匹配行右表字段为 NULL，普通等值条件不会为真，可能把这些行过滤掉。若要保留左表所有行，匹配条件常应放在 ON 中，但仍需结合实际业务验证。','画一个两表小例子，解释为什么总金额被放大。',['声明每张表的粒度','手算连接后的行数','区分 ON 与 WHERE 的作用']),
 'eng-transaction':D('事务处理一组操作的整体一致性，幂等处理重复执行。两者解决的问题不同。',[
 '扣库存和写订单需要避免只成功一个：可放在合适的事务边界。','客户端重试同一请求：服务端用幂等键识别同一业务意图。','同键不同参数应拒绝或明确处理，不能返回不相关的旧结果。'],
 '数据库事务为什么不能直接撤销已经发出的邮件？','邮件发送属于外部系统副作用，普通本地事务不能覆盖它。可采用事务内写 outbox、异步可靠发送、去重等方式协调，并明确重试与最终一致性的语义。','用付款超时重试解释事务与幂等各负责什么。',['区分部分成功与重复成功','说明幂等键作用域','识别外部副作用边界']),
 'eng-delivery':D('发布把代码带到不同的配置、权限和网络环境。启动成功只是第一步，还要确认关键任务能完成。',[
 '本地用开发地址，线上需要正确的资源基路径和 API 配置。','构建后检查静态资源、关键路由、失败状态和日志。','保留可回滚版本与数据库变更策略，部署失败时按已定义步骤恢复。'],
 '为什么把密钥放到前端环境变量里仍会泄露？','被打包进前端的值可被浏览器用户读取，变量名或构建替换不会提供保密性。需在服务端持有凭证，并执行权限、限流和审计。','给第一次接手项目的人讲清启动、验证、回滚三步。',['识别环境差异','核对权限与凭证位置','定义上线后的验收与恢复']),
 'math-vectors':D('向量把多个数量放在有顺序的坐标中。形状决定哪些运算有意义，坐标的业务含义决定结果能否解释。',[
 'x=[2,3]、w=[4,5]，点积 x·w=2×4+3×5=23。','X 有 3 个样本、每个 2 个特征，形状为 (3,2)；w 为 (2,1)，Xw 为 (3,1)。','如果把样本轴与特征轴对调，数值运算也许能凑上，但预测语义已变。'],
 '点积和余弦相似度什么时候排序不同？','点积受方向和长度共同影响，余弦除以两个向量的长度，关注方向。若所有向量已单位归一化，两者相同；零向量的余弦需要单独定义处理。','不用“矩阵乘法规则”作结，解释为什么中间维度必须对应。',['赋予每个轴明确含义','手算一组乘积','区分方向与长度']),
 'math-prob':D('条件概率先限制讨论的人群，再计算比例。改变条件会改变分母，因此 P(A|B) 一般不等于 P(B|A)。',[
 '1000 人里 10 人会购买，检测命中其中 8 人。','990 位不购买者里误报 99 人，共 107 人被预测会购买。','预测为正的人真实购买概率是 8/107≈7.48%，不是 80%。'],
 '同一个检测器在不同基础比例的人群中，精确率为什么可能变化？','P(A|B)=P(B|A)P(A)/P(B)。基础比例 P(A) 改变，即使灵敏度和误报率不变，预测正例中的组成也会变化。需区分条件概率与模型的校准概率。','向营销同事解释为什么命中率高仍可能浪费大量触达。',['写清条件事件','明确分子与分母','解释基础比例的作用']),
 'math-grad':D('导数描述一个输入在当前位置发生微小变化时，输出如何变化；梯度把多变量的偏导排成向量。',[
 'L(w)=(2w-6)²。先令 u=2w-6，再算 L=u²。','dL/dw=(2u)×2=4(2w-6)，体现链式法则。','w=1 时梯度为 -16；微小增加 w 会使损失下降。'],
 '数值差分为何可检查梯度，又不宜总替代反向传播？','用 [L(w+h)-L(w-h)]/(2h) 近似导数。h 太大会有截断误差，太小会有浮点抵消；高维参数逐个扰动代价大。不可微点也要谨慎比较。','解释梯度的符号如何指导一次局部调整。',['拆开复合函数','写出链式相乘','说明局部近似和数值误差']),
 'math-opt':D('梯度下降沿当前梯度的反方向迈一步。步长决定走多远，不保证一步就到最低点。',[
 'L(w)=(w-3)²，梯度 2(w-3)，从 w=0 开始。','学习率 0.1：w 新值为 0-0.1×(-6)=0.6，损失从 9 到 5.76。','对这一个二次函数，学习率 1 会在 0 和 6 间震荡；更大可能发散。'],
 '为什么训练损失每一步都下降不是随机梯度训练的必要现象？','小批次梯度有噪声，各批数据不同，单步损失可能波动。应看适当窗口趋势、验证集、学习率与梯度状态；不要把波动都解释为 bug，也不要忽略持续发散。','用具体数字讲一次更新，再解释步长过大的失败。',['手算参数更新','区分目标与更新规则','说明批次噪声与学习率']),
 'math-stats':D('样本效果是一次有限观察，统计推断要描述这种观察的不确定性。随机分组、样本量和停止规则都会影响解释。',[
 'A 组 1000 人中 100 人购买，B 组 1000 人中 120 人购买。','绝对差为 2 个百分点，相对提升为 20%；二者不能混用。','独立大样本近似下，差值标准误约 0.014；粗略 95% 区间约为 -0.007 至 0.047，不能仅凭点估计下结论。'],
 'p 值 0.03 是否表示“结论错误的概率是 3%”？','不是。p 值是在零假设及统计模型成立时，得到至少同样极端数据的概率；它不是假设为真的后验概率。多次查看后择机停止、多指标挑选也会改变错误率。','解释为什么 +20% 不一定足以推动上线。',['分清绝对与相对效果','陈述抽样与模型假设','同时讨论不确定性和业务代价']),
 'math-info':D('信息论用概率衡量惊讶程度。分类交叉熵惩罚给正确类别过低的概率，并不直接等于准确率。',[
 '真实类别为正，模型给正类概率 p，损失为 -ln(p)。','p=0.8 时约 0.223，p=0.2 时约 1.609。','两条样本都被分对，也可能因为置信度不同而有不同交叉熵。'],
 '交叉熵 H(p,q) 与 KL(p||q) 有什么关系？','H(p,q)=H(p)+KL(p||q)。固定目标分布 p 时，最小化交叉熵等价于最小化这一方向的 KL。KL 一般不对称；若 q 在 p 有质量处为零，理论值可为无穷。','解释损失为什么会特别惩罚自信地分错。',['用概率算一个损失','区分准确率与分布损失','说明 KL 的方向与条件']),
 'ml-problem':D('机器学习任务先定义样本、可用信息与预测目标。标签设计错误时，更复杂的模型只会更擅长回答错误问题。',[
 '任务：在发券前预测用户未来 7 天是否购买。','一行是一次发券决策；特征必须在决策时可获得。','发券后产生的点击不能做事前特征；基线可先用历史购买率或简单规则。'],
 '预测谁会购买，为什么不等于预测优惠券对谁有效？','购买倾向与干预增量不同：有些高倾向用户不发券也会买。后者涉及反事实和因果设计，通常需要随机实验或明确可辩护的识别假设。','先不用任何算法名，定义一个能被验证的营销预测任务。',['明确样本与预测时点','隔离未来特征','区分预测相关性与干预增量']),
 'ml-splits':D('切分模拟未来如何使用模型。验证时偷偷看到了部署时未知的信息，就会高估能力。',[
 '按时间预测未来：用早期训练、中期验证、后期测试。','标准化均值只从训练集计算，再用于验证和测试。','若同一用户多行且目标是泛化到新用户，应考虑按用户分组，而非任意拆行。'],
 '交叉验证之前先对全部数据做特征筛选，为什么泄漏？','筛选使用了未来验证折的信息，即使最后模型只拟合训练折也已间接受益。将预处理与筛选放入每一折的训练流程；最终测试集保留到决策完成之后。','解释一项看似无监督的预处理也可能泄漏。',['让切分匹配部署','仅训练部分拟合预处理','保持最终测试独立']),
 'ml-metrics':D('评价指标把错误分成不同类型。阈值越低通常召回越多，也可能带来更多误报，取舍取决于代价。',[
 'TP=8、FP=2、FN=4：precision=8/10=0.8。','recall=8/12≈0.667；F1=2TP/(2TP+FP+FN)=16/22≈0.727。','如果漏报代价远大于误报，单看 F1 未必符合业务目标。'],
 '类别极不平衡时，准确率 99% 为什么可能没有价值？','若正类仅 1%，全预测负类已有 99% 准确率，却完全找不到正类。检查混淆矩阵、PR 表现、校准与阈值下成本，并与基线在相同数据上比较。','拿一组小数据向同事解释 precision 与 recall 的分母。',['手算混淆矩阵指标','解释阈值和代价','与类比例基线比较']),
 'ml-linear':D('线性模型先把特征加权相加。逻辑回归再用 sigmoid 把分数映射到概率，线性部分在对数几率空间中。',[
 'z=w·x+b；x=2、w=0.5、b=-1 时 z=0。','sigmoid(0)=0.5；若标签 y=1，单样本交叉熵对 z 的梯度为 p-y=-0.5。','对 w 的梯度为 (p-y)x=-1；学习率 0.1 时 w 增加 0.1。'],
 '为什么特征缩放会影响优化与正则化的含义？','同样大小的权重面对不同尺度特征代表不同贡献，梯度量级也不同。L2 对参数大小的惩罚会受到尺度影响；缩放参数必须只在训练数据上拟合。','从加权和讲到概率，再手算一次参数改变。',['区分分数与概率','用链式法则解释梯度','说明缩放与正则化']),
 'ml-trees':D('树通过条件划分数据，让子节点更一致。单棵深树容易追随训练样本的细节，集成用不同方式减轻问题。',[
 '四条记录标签为 [0,0,1,1]，父节点 Gini=1-(0.5²+0.5²)=0.5。','某个阈值把两类完全分开，两个子节点 Gini 都为 0。','若划分后各是 [0,1]，加权 Gini 仍为 0.5，这次划分没有收益。'],
 '随机森林与梯度提升为何不能都叫“多棵树投票”？','随机森林通过随机性和聚合降低相关误差；梯度提升按当前损失的改进方向依次增加弱学习器。它们的依赖顺序、目标和调参方式不同。','手算一个划分收益，再解释树深度为什么影响泛化。',['计算加权不纯度','区分训练拟合与泛化','区分并行聚合与逐步提升']),
 'ml-unsupervised':D('无监督方法寻找数据结构，但结构由距离、尺度和目标共同决定。分成几群不等于发现了天然用户类型。',[
 '两个特征：消费额 0–10000，访问次数 0–10。','直接欧氏距离常被金额支配；缩放会改变邻近关系。','K-means 最小化到中心的平方距离，PCA 最大化保留的线性方差，目标不同。'],
 'PCA 方差保留 95%，是否保证保留 95% 的预测信息？','不保证。方差大的方向可能是无关背景，方差小的方向可能带有标签信号。降维应在训练集拟合，并通过下游任务和可解释性评估。','解释为什么聚类前的尺度选择本身就是建模假设。',['明确距离与目标','区分聚类与降维','用下游验证检查有用性']),
 'ml-recommend':D('推荐把庞大的候选空间逐步缩小。候选召回、排序和展示各有损失，最终反馈也受展示策略影响。',[
 '先从一百万商品召回一千，再排序选十个展示。','没进入候选的好商品，排序模型无法补救。','日志只记录被展示商品的点击；未展示不代表用户不喜欢。'],
 '离线点击率模型提高，为什么线上收益可能下降？','可能存在曝光偏差、目标与长期价值不一致、位置效应或分布变化。分别检查召回、排序、业务约束和实验护栏，不能把观察日志中的相关性直接当干预效果。','沿着候选、排序、展示、反馈讲清一条推荐链路。',['拆开阶段指标','识别未观察反馈','考虑线上目标与偏差']),
 'dl-tensors':D('张量是带多个轴的数据。批次、时间、通道等轴的含义先于任何网络层，广播只看形状兼容，不懂业务。',[
 'X 形状 (2,3)，W 形状 (3,4)，矩阵乘积为 (2,4)。','加偏置 b 形状 (4,) 时，广播到每个样本。','(2,1) 与 (2,) 相加会广播成 (2,2)，未必是你期待的逐样本相加。'],
 'reshape 为什么不能代替 transpose？','reshape 重新解释元素在形状中的排列；transpose 交换轴的位置。对于 2×3 矩阵，简单 reshape 为 3×2 与真正转置通常内容不同，需打印小矩阵核对。','用一个两样本例子解释轴和广播为什么会悄悄出错。',['标出每个轴','手算广播后的形状','区分重塑与换轴']),
 'dl-backprop':D('反向传播沿计算图把局部导数相乘并累加。优化器再使用这些梯度更新参数，两者不是同一个步骤。',[
 'x=2、w=1、y=5，预测 z=wx=2，损失 L=(z-y)²=9。','dL/dz=2(z-y)=-6，dz/dw=x=2，所以 dL/dw=-12。','学习率 0.1 时 w←1-0.1×(-12)=2.2，新预测 4.4，新损失 0.36。'],
 '参数通过两条路径影响同一个损失时，梯度怎么合并？','链式法则对所有路径的贡献求和。多数训练框架还会把多次 backward 的梯度累积到参数梯度中；常规独立批次要清零，有意累积时需统一损失缩放和更新时机。','从数值前向开始，逐个局部导数讲到参数更新。',['写出中间量与导数','区分求导和更新','说明多路径与跨批次累积']),
 'dl-regularize':D('训练误差下降说明更适应训练数据，验证表现才帮助判断能否推广。正则化约束学习过程，不能补救泄漏。',[
 '训练损失持续下降，验证损失先降后升。','用验证集选择早停点，测试集只用于最终评估。','Dropout 训练时随机屏蔽并缩放激活，评估时关闭；模型模式应明确切换。'],
 'BatchNorm 的 eval 模式与关闭梯度有什么区别？','eval 改变 Dropout、BatchNorm 等模块行为；no_grad 控制自动微分记录。二者解决不同问题，推理时常一起使用。不同模型和归一化配置还需核对具体语义。','解释为什么训练得更久可能在新样本上更差。',['比较训练与验证曲线','正确使用模式与梯度开关','避免用测试集选早停']),
 'dl-cnn':D('卷积在不同位置复用同一组局部权重，使模型利用局部模式。权重共享降低参数量，但不自动保证所有变换不变性。',[
 '输入 [1,2,3,4]，核 [1,-1]，步幅 1、无填充。','按深度学习常用的互相关运算，输出依次为 -1、-1、-1。','长度为 4-2+1=3；步幅或填充改变后要重新核对合法窗口。'],
 '一个卷积层的参数量为何通常不随图像宽高增加？','标准卷积核参数由输出通道×输入通道×核高×核宽决定，另可有偏置；空间位置复用这些参数。计算量和激活内存仍会随空间尺寸增加。','用滑动小窗口解释共享权重与输出尺寸。',['手算窗口结果','说明步幅与填充','区分参数量和计算量']),
 'dl-sequence':D('循环网络用状态概括已看到的序列。状态传递引入顺序，也让远处信息依赖多次变换才能影响当前输出。',[
 '简化递推 h_t=0.5h_(t-1)+x_t，h_0=0，输入为 1、2、3。','依次得到 h_1=1、h_2=2.5、h_3=4.25。','第一个输入对后面状态的影响乘上多个 0.5，距离增加时逐渐衰减。'],
 '为什么门控结构能帮助长依赖，却不保证无限记忆？','门控让信息保留与更新可学习，提供较顺畅的梯度路径；效果仍受门值、数据、长度和优化影响。评估时要设计真正需要远距离信息的任务。','解释顺序状态为何可能遗忘较早输入。',['逐步计算状态','追踪跨步梯度因子','说明门控帮助与限制']),
 'dl-attention':D('注意力让当前位置按相关程度混合其他位置的内容。Q 与 K 决定权重，V 提供被混合的信息。',[
 'q=[1,0]，k1=[1,0]、k2=[0,1]，原始分数为 1、0。','除以 √2 后做 softmax，权重约为 0.670、0.330。','若 v1=[2,0]、v2=[0,4]，加权结果约为 [1.340,1.321]；因果 mask 会先排除不可看的未来位置。'],
 '标准稠密自注意力为什么随序列长度变得昂贵？','每个位置与所有位置比较，分数矩阵有 n² 项，核心计算约 O(n²d)。具体实现可减少显式存储和访存，但不能笼统把所有注意力都说成线性。','用两条记录解释 Q、K、V 和归一化各做什么。',['手算分数和归一化','区分权重与内容','说明 mask 与长度代价']),
 'dl-selfsupervised':D('自监督从数据本身构造学习目标，例如预测被遮蔽内容或匹配不同视图。没有人工标签不等于没有目标和偏差。',[
 '把同一图像的两个合理增强视为正对，不同图像作比较对象。','对比损失提升正对相对分数，并与候选集合中的其他分数比较。','如果增强抹去了任务关键含义，学到的不变性可能损害下游表现。'],
 '所有输入映射成相同向量为什么是表示塌缩？','表示不再保留区分样本的信息。有的方法使用负样本，有的方法通过非对称结构、停止梯度、方差约束等避免塌缩；不能把一种机制当成所有自监督方法的定义。','解释学习信号从哪里来，以及如何检验表示是否有用。',['定义目标与正对假设','识别塌缩','通过独立下游任务验证']),
 'dl-generative':D('生成模型学习产生数据的机制。不同家族用不同训练目标，不能只凭输出图片相似就认为原理相同。',[
 '扩散前向加噪可写作 x_t=√ᾱ_t x_0+√(1-ᾱ_t) ε。','若 x_0=2、ᾱ_t=0.25、ε=1，则 x_t=1+√0.75≈1.866。','常见训练目标让网络在给定 t 和 x_t 时预测噪声；采样再按所用算法逐步生成。'],
 'VAE、GAN 和扩散模型在目标上有什么核心区别？','VAE 优化含重建与潜变量分布约束的变分下界；GAN 使用生成器和判别器的对抗目标；扩散常学习不同噪声尺度的去噪或相关目标。训练目标与实际采样过程都要分别核对。','先解释一个加噪步骤，再比较生成目标。',['写清噪声和时间条件','区分训练与采样','准确区分三个模型家族']),
 'llm-tokens':D('语言模型在已有上下文条件下预测下一个 token。上下文像本次任务可参考的材料，权重像训练形成的参数；这只是帮助区分生命周期的类比。',[
 '给两个候选 token 分数 [2,1]，温度 1 的 softmax 概率约 [0.731,0.269]。','温度 0.5 将分数除以 0.5，概率约 [0.881,0.119]，分布更集中。','如果最高分候选本来就不符合事实，集中概率不会把它变正确。'],
 'KV cache 保存了知识库答案吗？为什么长上下文会占更多内存？','KV cache 复用历史位置的注意力中间计算，不是验证过的答案库。常规自回归注意力实现中，需要保存更多位置、层、头相关张量，长度与并发会影响内存。具体布局由模型和实现决定。','向 Nico 解释给模型新资料和训练模型的区别。',['区分 token 与字符','区分推理上下文和权重更新','说明采样与事实可靠性的区别']),
 'llm-context':D('上下文设计是在有限空间内保留完成任务所需证据与约束。更短不总是更省：缺信息造成返工也花成本。',[
 '目标：修复 0 库存显示；提供输入 0、当前输出、期望输出。','保留相关组件、接口、不可改动范围和验证案例，去掉重复无关日志。','完成一轮后只带走已验证改动、剩余问题和关键限制，再进行下一步。'],
 '如何比较两个提示模板是否更省 token？','对相同代表任务记录端到端输入输出 token、重试、成功率、时间与人工修正。先固定验收标准再比较；只比较第一条消息长度会漏掉返工。','解释一份最小但足够的修复任务包要包含什么。',['提供可重现目标和证据','保留否定约束与未完成项','比较端到端质量和成本']),
 'llm-tools':D('工具调用让模型提出结构化动作，系统检查是否允许执行。看起来规范的 JSON 仍可能包含越权或错误参数。',[
 '模型提出 search_notes(query="库存", limit=5)。','程序检查工具名、字段类型、limit 范围和当前用户文档权限。','只把可读结果返回模型；检索结果中的指令也不能替用户扩大权限。'],
 '参数符合 schema，为什么仍可能执行了错误动作？','schema 只覆盖部分结构限制，无法独自表达全部业务权限、对象归属、余额或重复执行语义。必须在可信执行边界做动态检查，并记录实际结果。','讲清模型提议与系统执行之间必须经过哪些检查。',['区分结构与业务校验','明确权限检查位置','考虑重复调用和不可信结果']),
 'llm-eval':D('评估先定义好结果和失败类型，再用保留案例比较版本。看一次成功演示无法估计真实稳定性。',[
 '准备有答案、无答案、越权、冲突资料四类案例。','每类分别检查正确性、引用支持、拒答和工具行为，同时记录延迟与成本。','改动后用相同保留案例比较，开发案例和最终保留集分开。'],
 '让同一个模型评价自己的回答，有哪些证据不足？','评审可能受措辞、顺序、共同偏差影响；模型分数不是天然真值。用明确 rubric、可执行检查、人审样本和盲评校准，报告不同失败类型而非只报均分。','设计一个能发现“引用存在但不支持结论”的评估案例。',['定义可检查标准','隔离调参与保留案例','用多种证据校准评审']),
 'llm-rag':D('RAG 先找证据再生成回答。检索错和生成错是两类失败，修复办法不同。',[
 '问题：退货时限？检索得到文档 A 的 7 天规则和文档 B 的过期规则。','先确认权限、版本和片段能否直接支持回答。','回答附引用；若没有有效证据，保留“无法从当前资料确认”的出口。'],
 '召回文档包含正确答案，为什么最终回答仍会错？','模型可能误读条件、采用旧版本或拼接不兼容信息。分别测检索命中、证据支持、条件覆盖与答案正确性；关键词小实验只验证检索流程，不代表已经实现语义 RAG。','用一次失败拆解检索与生成各自该检查什么。',['追踪来源与版本','区分检索和生成错误','为证据不足设计明确出口']),
 'llm-agent':D('Agent 在观察结果后决定下一步动作。循环越开放，越需要明确预算、状态、失败恢复和停止条件。',[
 '任务最多 3 步：检索、核对、整理；每次记录动作和观察。','无证据时可改写查询，但重复相同失败两次应停止或请求帮助。','预算耗尽时返回已完成部分与未解决事项，而非继续无界尝试。'],
 '固定工作流什么时候比开放 Agent 更合适？','步骤稳定、验收明确时，固定流程更容易预测和测试。需要依观察动态选择路径时才考虑更开放控制；工具调用数量和自由度应由任务证据决定。','解释一个会停止的循环，以及它如何处理工具失败。',['定义状态和观察','给出步数与成本预算','区分固定流程和动态决策']),
 'llm-finetune':D('微调通过训练数据改变参数行为。LoRA 在某些权重旁学习低秩增量，减少可训练参数；它不自动清除所有训练内存开销。',[
 '权重 W 为 m×n，增量 ΔW=BA，B 为 m×r，A 为 r×n。','m=n=1024、r=8 时，增量参数为 8×(1024+1024)=16384，原矩阵为 1048576。','前向仍需要基础模型与激活；参数减少比例不等于总显存减少比例。'],
 '应该用 RAG 还是微调解决“公司规则每周更新”？','经常变化且需引用的事实通常先考虑外部检索；稳定格式、风格或任务行为可评估微调。先建立提示和检索基线，再用保留数据证明训练带来收益。','从矩阵形状解释低秩适配，然后说明什么问题不适合靠它解决。',['推导可训练参数量','区分参数与总内存','通过基线和保留集决定训练价值']),
 'llm-alignment':D('偏好优化让模型更倾向人类或评审偏好的回答。偏好信号只是目标代理，仍可能被投机利用。',[
 '一个提示有 chosen 与 rejected 两个回答。','DPO 比较策略相对参考模型对两回答的对数概率差，并用偏好损失推动正确方向。','提高相对偏好不等于确保事实正确；还需检查安全、能力退化和长度等捷径。'],
 'DPO 与基于 PPO 的 RLHF 为什么不能当同一个训练循环？','常见 PPO-RLHF 包含奖励建模和采样策略优化；DPO 用偏好对直接优化策略目标，通常不需要显式训练一个奖励模型作为该循环步骤。数据、参考策略和假设仍影响结果。','讲清偏好数据如何变成优化信号，以及这种信号不证明什么。',['区分偏好与事实真值','描述相对参考策略的比较','区分 DPO 与 PPO 流程']),
 'rl-bandits':D('多臂老虎机研究反复选择的探索与利用。每次行动只观察被选项的反馈，没试过不能简单当作差。',[
 '某动作已试 4 次，平均奖励 Q=0.5，新奖励 r=1。','增量均值 Q←0.5+(1-0.5)/5=0.6。','始终选择当前最大均值可能错过被低估的动作；探索要付出短期代价。'],
 '环境奖励分布变化时，用历史总均值有什么问题？','很久以前的数据权重累积会让适应缓慢。固定步长更新对近期数据更敏感，但也保留噪声；应根据平稳性与评价目标选择，不把探索当无成本行为。','用两种猫粮解释为什么要尝试不确定的选项。',['只使用被选择动作的反馈','手算增量均值','说明探索代价与非平稳性']),
 'rl-mdp':D('MDP 把问题拆成状态、动作、转移、奖励和回报。状态应包含预测后续所需信息，奖励表达的是你真正想优化的行为。',[
 '三步奖励 [0,0,1]，折扣 γ=0.9，起点回报为 0+0.9×0+0.9²×1=0.81。','同一即时奖励可能通向不同未来，因此不能只贪眼前分数。','真实终止后没有后续回报；时间截断是否继续估值，取决于任务定义与观测信息。'],
 '只记录棋子位置，遗漏“钥匙是否拿到”，为什么状态可能不充分？','同一位置下可用行动与未来奖励因钥匙状态不同而改变。需要把相关历史信息纳入状态，或使用处理部分可观测性的机制。','给 Nico 的小世界写出完整状态和奖励含义。',['明确状态的充分信息','手算折扣回报','区分终止与截断']),
 'rl-q':D('Q-learning 用一次经验调整某个状态动作的长期价值估计。目标既含即时奖励，也含下一状态的最好估值。',[
 '当前 Q=1，奖励 r=2，下一状态最大 Q=3，γ=0.9。','非终止目标为 2+0.9×3=4.7，TD 误差 3.7。','α=0.1 时新 Q=1.37；若是真实终止，目标只为 2，新 Q=1.1。'],
 '为何行为用 ε-greedy，目标仍取 max Q，叫离策略？','收集数据的行为策略包含探索，目标对应的是对 Q 贪心的策略，两者不同。相比之下 SARSA 使用实际下一动作的价值；收敛还依赖覆盖、步长等条件。','手算有终止和无终止两次更新，并解释差异。',['写出 TD 目标和误差','终止时移除 bootstrap','区分行为策略与目标策略']),
 'rl-dqn':D('DQN 用神经网络近似 Q 值。经验回放减弱连续样本相关性，目标网络减缓训练目标同步漂移。',[
 '在线网络给当前动作估值 Qθ(s,a)。','目标常写为 r+γ(1-done) max_a Q_target(s′,a)。','对目标停止梯度，只更新在线网络；按策略同步目标网络参数。'],
 'Double DQN 怎样减轻 max 操作带来的过估计？','在线网络选择下一动作 argmax，目标网络评估该动作价值，将选择与评价分开。它不保证完全无偏，也不能解决所有不稳定和数据覆盖问题。','解释为什么目标网络不是第二个独立智能体。',['区分在线与目标网络职责','说明 replay 的作用','描述 Double DQN 的选择与评价']),
 'rl-policy':D('策略梯度直接调整行动概率。把好于预期的动作变得更可能，把差于预期的动作变得更不可能。',[
 '策略给动作概率 0.2，观察到正优势 A=3。','优化目标包含 log π(a|s)×A，沿增加该动作对数概率的方向更新。','减去不依赖动作的状态基线可降低梯度估计方差，理想条件下不改变期望方向。'],
 'Actor-Critic 中 critic 估错会怎样影响 actor？','critic 用于估计价值或优势，错误会给策略更新带来偏差或噪声。需平衡价值学习、采样和策略更新，不能把更低价值损失等同于更好最终策略。','不用公式先讲优势，再对应到 log 概率与更新方向。',['区分概率和价值','解释基线与优势','说明 critic 误差的影响']),
 'rl-ppo':D('PPO-Clip 用新旧策略概率比约束部分更新激励。裁剪的是代理目标中的收益，不是把全部参数锁进硬边界。',[
 '旧概率 0.2，新概率 0.3，比率 r=1.5；ε=0.2，优势 A=2。','未裁剪收益 3，裁剪收益 2.4，取 min 得 2.4，减少继续提高这一好动作概率的激励。','若 A=-2，min(-3,-2.4)=-3；不能把所有情况都按正优势推断。'],
 '为什么 PPO 裁剪不保证 KL 距离一定小？SAC 又改变了什么目标？','裁剪是采样代理目标的局部机制，整体策略仍可能变化过大，因此实现常监测 KL 等信号。SAC 属于最大熵、离策略 actor-critic 路线，在奖励之外鼓励策略熵，不是简单换一个裁剪参数。','分别手算正负优势，解释 PPO 想限制什么、没有保证什么。',['正确处理正负优势','区分裁剪激励与硬约束','区分 PPO 与 SAC 的路线']),
 'rl-offline':D('离线强化学习只能使用已有数据，无法随时试验模型看好的新动作。分布外动作的估值误差可能被策略放大。',[
 '日志只在某状态记录动作 A，几乎没有动作 B。','函数逼近可能给 B 一个虚高 Q，贪心策略于是选择 B。','离线训练不能立刻通过环境反馈纠错，需要覆盖分析、保守性或行为约束等方法。'],
 '有了学习到的环境模型，就能任意长时间规划吗？','模型误差会随多步滚动累积，优化器还可能利用模型缺陷。需要不确定性估计、短滚动、真实数据校验等；多智能体还可能使环境随对手策略变化。','解释“日志里没失败过”为什么不证明新策略安全有效。',['识别分布外动作','说明模型误差累积','报告数据覆盖和离线评估限制'])
};
