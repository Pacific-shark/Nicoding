import {lesson as L,question as Q,challenge as C} from './schema.js';
export const ml = [
L('ml-problem','监督学习与基线',"定义样本、预测目标和一个简单基线。",['math-vectors'],[
 ["样本、特征、标签", "一条样本可以是“用户在某个时刻”；特征必须是当时已知的信息；标签是未来是否购买。预测金额是回归，预测类别是分类。定义时间窗口后再造数据。"],
 ["基线使复杂度有比较对象", "总预测平均值、众数或简单规则，是便宜而必要的参照。深模型若没有超越基线，先检查数据与评估，不要默认继续加层。业务代价决定哪些错误更重要。"],
 ["为一条样本标出时间", "如果在周一预测某用户未来七天是否购买，特征只能使用周一决策时已经知道的浏览、历史购买等信息。周五产生的支付状态不能回填成这条样本的输入。\n\n均值或众数基线也要从训练数据确定，再到独立数据上评估。接下来的均值代码只演示训练样本内的误差，不是上线效果。"]
],`y = [10, 20, 30]
baseline = sum(y)/len(y)
mse = sum((baseline-v)**2 for v in y)/len(y)
print(baseline, round(mse, 2))`,'均值基线 20，训练样本上的 MSE 约 66.67；泛化需另测。',['预测时不可获得的字段不能当特征。','标签可能只是业务目标的代理，先看它是否会误导优化。'],[
 Q('预测“未来 7 天是否购买”属于？',['分类','无标签聚类','文件压缩'],0,['正确，输出购买/不购买类别或其概率。','已定义监督标签。','不属于此任务。']),
 Q('复杂模型首先应和什么比较？',['可信的简单基线','只有它自己的训练分数','模型名称长度'],0,['正确，没有参照无法判断复杂度是否有价值。','自我对比不够。','名称不反映效果。'])
],['ml'],'把一个业务问题写成样本单位、特征截止时刻、标签窗口和基线。'),
L('ml-splits','数据切分与泄漏',"按部署场景切分数据，避免信息泄漏。",['ml-problem','eng-sql'],[
 ["训练、验证、测试各有职责", "训练集拟合参数，验证集做选择，测试集提供最终独立估计。反复看测试结果改模型，测试集也变成了选择工具。时间预测应按时间切分，重复用户可能需要分组切分。"],
 ["预处理也会泄漏", "均值、标准差、缺失填充、特征选择要只在训练数据拟合，再应用到验证和测试。即使没有直接读标签，先用全量数据做变换也可能把未来信息带进来。"],
 ["标准化也要遵守切分", "训练值为 10、20、30，均值是 20。测试值 100 应减去已经拟合好的 20，得到 80。若先把四个数混在一起算均值，就用到了测试信息。\n\n同一用户反复出现时，还要问部署是预测已有用户的新记录，还是预测从未出现的用户。两种目标可能需要不同的切分方式，随机打乱不是通用答案。"]
],`train = [10, 20, 30]
test = [100]
mean_train = sum(train)/len(train)
print("训练均值:", mean_train)
print("测试变换:", [x-mean_train for x in test])`,'训练均值为 20；测试结果 80，测试值没有参与均值拟合。',['随机切分不总合理，要匹配部署数据如何到来。','用户 ID、订单状态和时间字段都可能隐藏标签泄漏。'],[
 Q('标准化均值应在哪个集合拟合？',['全量数据','训练集','测试集'],1,['会让评估数据参与预处理选择。','正确，随后固定变换应用到其他集合。','测试集不能提供训练信息。']),
 Q('预测未来订单时，最应检查哪种切分？',['按时间的评估','随便打乱即可','按金额从小到大后交错'],0,['正确，要模拟先看到过去再预测未来。','可能造成未来信息进入训练。','不一定对应部署过程。'])
],['split'],'检查现有训练流程中每个 fit 是在哪一份数据上执行的。',{priority:1}),
L('ml-linear','线性与逻辑回归',"区分线性分数、概率与分类阈值。",['ml-splits','math-opt','math-info'],[
 ["先算线性分数", "z=w·x+b 用权重组合特征。线性回归直接预测连续值，常用平方误差；逻辑回归用 sigmoid 把 z 映射到 0 到 1，常用交叉熵训练二分类。"],
 ["阈值是业务决策", "概率 0.6 不天然等于“必须触达”。阈值取决于误报、漏报和成本，且概率要评估校准。线性模型系数受特征尺度、相关性和正则影响，不能直接解读成因果效应。"],
 ["分数、概率和决定分开看", "线性分数 z=0 经 sigmoid 得到 0.5，z=2 时约为 0.8808。模型输出一个概率形式的数值后，还要另选触达阈值。\n\n一次触达成本很高时，阈值可能需要提高；漏掉高价值用户更贵时，可能接受更多误报。系数和阈值都不能直接解释成因果效果。"]
],`import math
sigmoid = lambda z: 1/(1+math.exp(-z))
for z in [-2, 0, 2]:
    print(z, round(sigmoid(z), 4))`,'得到约 0.1192、0.5、0.8808。这是分数到概率形式的映射。',['逻辑回归虽然叫回归，常用于分类。','sigmoid 输出并不自动保证现实概率校准。'],[
 Q('sigmoid(0) 是多少？',['0','0.5','1'],1,['函数值不等于输入。','正确，1/(1+1)=0.5。','有限输入下不是 1。']),
 Q('系数为正能证明该特征导致购买吗？',['能','不能，只是模型中的关联','说明数据必定无偏'],1,['相关与因果不能等同。','正确，因果需要额外设计与假设。','系数符号不证明无偏。'])
],['ml'],'比较同一个触达模型在两个阈值下的预算和错误代价。'),
L('ml-metrics','精确率、召回率与阈值',"根据混淆矩阵计算指标，并解释错误代价。",['ml-splits','math-prob'],[
 ["从四格表开始", "TP 是正确找到的正例，FP 是误报，FN 是漏报，TN 是正确排除。精确率 TP/(TP+FP) 问“找出的有多准”，召回率 TP/(TP+FN) 问“应找出的找到了多少”。"],
 ["类别不平衡改变直觉", "正例只有 1% 时，全预测负也能有 99% 准确率。根据业务看 PR、ROC、阈值代价、校准和分群表现。这里约定分母为零时指标返回 0；报告时应说明约定。"],
 ["用一百条结果核对分母", "在 100 人中，模型选出 10 人，8 人实际购买，2 人没购买。另有 12 位购买者没有被选中，剩余 78 人既没被选中也没购买。\n\n精确率是 8/10=0.8，问选出的人里有多少真正购买；召回率是 8/(8+12)=0.4，问全部购买者找到了多少；准确率是 (8+78)/100=0.86。三个比例各有分母，不能互换。"]
],`tp, fp, fn, tn = 8, 2, 12, 78
print("precision:", tp/(tp+fp))
print("recall:", tp/(tp+fn))
print("accuracy:", (tp+tn)/(tp+fp+fn+tn))`,'精确率 0.8、召回率 0.4、准确率 0.86。每个指标回答不同问题。',['提高阈值通常减少正例预测，但具体指标要实际测量。','AUC 不直接给出某个预算下的业务收益。'],[
 Q('在所有被预测为正的样本中，真正为正的比例是？',['召回率','精确率','样本量'],1,['召回率的分母是真实正例。','正确，TP/(TP+FP)。','样本量是计数。']),
 Q('1% 正例，全预测负，召回率是多少？',['99%','0%','100%'],1,['这是准确率。','正确，一个真实正例都没找出。','没有召回正例。'])
],['metrics'],'给实际营销触达列出误报和漏报的代价，而不是先选一个通用指标。',{challenge:C('实现 precision(tp, fp)，分母为零时返回 0。',`def precision(tp, fp):
    return tp`,`def precision(tp, fp):
    return tp / (tp + fp) if tp + fp else 0`,['assert precision(8,2) == 0.8','assert precision(0,0) == 0','assert precision(3,0) == 1'],['精确率的分母是所有预测正例。','先处理 tp + fp 为零的边界。'])}),
L('ml-trees','决策树、随机森林与 Boosting',"理解树的划分方式与两类集成方法。",['ml-metrics'],[
 ["树逐层划分特征空间", "一个分裂可以是“浏览次数 ≤ 3”。算法选择能改进目标的分裂；叶子给出类别或数值。树太深会记住噪声，需要深度、叶子样本数或剪枝等约束。"],
 ["两种集成直觉", "随机森林通过不同样本和特征随机性训练多棵树再聚合，主要降低方差。梯度提升按顺序添加弱学习器去改进当前目标；平方误差下可以直观理解为拟合残差。它们不是所有数据上固定的胜负关系。"],
 ["残差修正是怎样累加的", "真实值为 [10,20,30]，当前预测全是 20，残差便是 [−10,0,10]。如果新的学习器恰好预测出这些残差，再乘学习率 0.5 加回旧预测，结果成为 [15,20,25]。\n\n平方误差下这种直觉很直接。其他损失的梯度提升要根据损失的梯度构造学习信号，不能把所有情况都当成直接拟合 y−预测值。"]
],`y = [10, 20, 30]
pred = [20, 20, 20]
residual = [a-b for a,b in zip(y,pred)]
print(residual)
print([p+0.5*r for p,r in zip(pred,residual)])`,'残差为 [−10,0,10]，半步修正得到 [15,20,25]；这里只演示平方误差的一步直觉。',['特征重要性不是因果贡献。','训练集拟合完美可能表示过拟合。'],[
 Q('平方误差下，Boosting 的残差直觉是？',['继续改进当前预测的误差','每次完全忘记旧模型','只打乱标签'],0,['正确，逐步添加修正。','之前模型仍参与总体预测。','打乱标签不是学习目标。']),
 Q('限制树深度主要帮助什么？',['控制模型复杂度','确保绝无偏差','把分类变成 SQL'],0,['正确，可减少过拟合但也可能欠拟合。','任何模型仍可能有偏差。','没有这种转换。'])
],['tree','boost'],'给一个表格任务比较简单规则、线性模型与树模型，使用同一切分和指标。'),
L('ml-unsupervised','聚类与 PCA',"观察距离、特征尺度与降维目标。",['math-vectors','ml-splits'],[
 ["K-means 在指定距离下分组", "给定 K 后，交替把样本分到最近中心，再更新中心。尺度会影响距离；消费额与访问次数不处理尺度，结果可能被较大数值的特征主导。"],
 ["PCA 寻找方差大的方向", "PCA 用正交方向压缩线性结构，通常先中心化，是否标准化取决于任务。保留大方差不等于保留所有与标签有关的信息；无监督结构需要业务解释与稳定性验证。"],
 ["分组之后还要检查什么", "点 [1,2,8,9] 配两个初始中心 1、9，按最近中心分成 [1,2] 和 [8,9]，新中心为 1.5、8.5。重复分配与更新，直到达到停止条件。\n\n这说明算法怎样分组，还没有说明两个组有业务意义。若把金额单位从元换成分，距离可能被金额维度主导；尺度处理与稳定性要另外检查。"]
],`points = [1, 2, 8, 9]
centers = [1, 9]
groups = [[x for x in points if min(range(2), key=lambda k: abs(x-centers[k])) == i] for i in range(2)]
print(groups)
print([sum(g)/len(g) for g in groups])`,'分组 [[1,2],[8,9]]，更新中心为 [1.5,8.5]。真实实现还需处理空簇。',['聚类标签编号没有天然顺序或价值高低。','PCA 的最大方差方向不一定最利于预测目标。'],[
 Q('特征单位差异大时，K-means 首先要考虑？',['距离与尺度','给簇起名字就行','标签必须是红色'],0,['正确，尺度影响最近中心判断。','命名不能修正距离失真。','颜色与算法无关。']),
 Q('PCA 保留的大方差一定最有预测力吗？',['一定','不一定，没使用标签目标','PCA 就是分类器'],1,['方差与预测相关性不同。','正确，需要结合下游任务验证。','PCA 本身是表示变换。'])
],['cluster','pca'],'为一个用户分群写出特征、尺度处理、K 的选择与业务验证方式。'),
L('ml-recommend','推荐、排序与反馈偏差',"理解召回、排序和曝光日志中的偏差。",['ml-metrics','ml-unsupervised'],[
 ["多阶段系统", "召回从大库找候选，排序估计相关性或业务价值，重排处理多样性与约束。离线 Recall@K、NDCG 与线上收益回答不同问题；不能互相替代。"],
 ["日志不是自然真相", "曝光位置影响点击，未曝光不等于不喜欢。训练和评估要考虑选择偏差、冷启动与流行度反馈。目标只优化点击，可能牺牲满意度、长期留存或内容多样性。"],
 ["一个排名指标的具体含义", "相关商品为 B、D，系统排出 A、C、B、D。只取前三名时命中了 B，两个相关商品找回一个，Recall@3=0.5。\n\n这个结果依赖相关商品如何定义、候选集合如何准备。历史没曝光的商品缺少点击机会，不能仅凭“没有点击”就断定用户不喜欢。"]
],`ranking = ["A", "C", "B", "D"]
relevant = {"B", "D"}
k = 3
hits = len(set(ranking[:k]) & relevant)
print("Recall@3:", hits / len(relevant))`,'Top3 命中 B，两个相关项中召回一个，Recall@3=0.5。',['评估候选集如何构建，会显著影响离线分数。','点击是反馈信号，不是完整的用户效用。'],[
 Q('没有曝光过的商品未被点击，能直接当作讨厌吗？',['能','不能，缺少观察机会','说明价格必定太高'],1,['未被看到不提供这种证据。','正确，这是反馈偏差的重要来源。','没有证据支持该原因。']),
 Q('召回阶段通常负责？',['从大库产生候选集合','最终渲染按钮','保证每个商品都展示'],0,['正确，再交给后续排序与约束。','这是界面职责。','召回就是缩小候选范围。'])
],['rec'],'给一个推荐场景设计候选来源、排序目标和防止单一指标伤害体验的护栏。'),
];
