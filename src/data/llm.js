import {lesson as L,question as Q} from './schema.js';
export const llm = [
L('llm-tokens','Token、预训练与推理',"区分分词、参数训练和逐 token 推理。",['py-containers'],[
 ["token 不等于一个汉字", "分词器把文本变成 token ID，不同模型的规则不同。空格、语言、代码格式都会影响长度。按字符数估算只能是粗略预算，计费或上下文校验应使用对应分词器与服务数据。"],
 ["训练与推理的区别", "预训练更新权重以学习目标分布；推理在给定上下文下计算下个 token 的分布并解码。上下文里告诉模型新事实，不等于永久改了权重。采样温度改变分布形状，不是事实正确率开关。"],
 ["一次回答怎样逐步生成", "模型拿到当前上下文，为可能的下一个 token 计算分数，解码过程选择其中一个，把它追加到序列，再预测下一项。直到遇到停止条件或长度限制。\n\n提示里补充文档会改变这次计算所见的信息，常规推理不会因此更新参数。温度改变选择的分布，降低温度也不能补上模型或上下文中缺失的事实。"]
],`import math
logits = [2.0, 1.0, 0.0]
for temperature in [0.5, 1.0, 2.0]:
    scaled = [x/temperature for x in logits]
    m = max(scaled)
    exps = [math.exp(x-m) for x in scaled]
    print(temperature, [round(x/sum(exps),3) for x in exps])`,'较低正温度使分布更集中，较高温度更平坦；这里不是实际语言模型输出。',['温度低仍可能自信地生成错误。','上下文窗口长度与可有效使用的信息量不是完全同一概念。'],[
 Q('在提示词里加入新文档，通常直接改变什么？',['本次上下文','永久模型权重','训练数据自动公开'],0,['正确，常规推理不会因此训练权重。','需要专门训练过程。','没有这种必然行为。']),
 Q('token 数能用中文字数精确代替吗？',['能','不能，取决于分词器和文本','只看段落数量'],1,['分词不是固定一字一 token。','正确，应按模型规则核算。','段落也不能精确计数。'])
],['llm'],'用所用模型对应分词器比较中文、英文、代码三段输入的 token 长度。'),
L('llm-context','上下文设计与任务分解',"组织相关信息，并保留任务约束和验收条件。",['eng-debug','llm-tokens'],[
 ["一份可执行任务说明", "给目标、必要背景、输入、约束、输出格式、验收标准。复杂任务拆成能独立验证的步骤，每一步保存紧凑结果和证据。不要把未经验证的中间结论当下一步事实。"],
 ["管理有限上下文", "优先保留接口契约、决策、当前错误与相关片段；历史闲聊可摘要。摘要标记假设和未完成事项，避免压缩后丢掉关键否定条件。成本看完整任务的总 token、返工次数和时间。"],
 ["给一次修改保留必要信息", "“把库存显示修好”没有说明错在哪里。更有用的描述是：StockLabel 收到 0 时显示 100，预期显示“售罄”；只改这个组件，核对输入 0、1、null。\n\n它短，是因为保留了定位和验收所需的信息。省略实际错误或关键约束虽然还能更短，却可能引来多轮猜测。比较成本时要把这些返工计算在内。"]
],`task = {"goal":"修复零库存显示", "scope":["StockLabel"],
        "input":0, "expected":"售罄", "verify":[0,1,None]}
for key, value in task.items():
    print(key, ":", value)`,'这份结构保留了目标、边界和例子，可作为你给 AI 的任务包骨架。',['要求模型“绝对准确”不提供验证机制。','让多个模型相互同意，也不自动构成独立事实证据。'],[
 Q('压缩上下文时最不该丢掉？',['接口契约与未解决约束','重复寒暄','无关旧日志'],0,['正确，这些决定结果是否符合任务。','通常可以压缩。','可按相关性删除或摘要。']),
 Q('评估省 token 的方法，应观察？',['整个任务的成本与质量','只看第一条提示词字数','只看回答是否自信'],0,['正确，返工可能抵消首次节省。','会漏掉后续反复。','自信不是成本或质量证据。'])
],['llm','mcp'],'为自己常做的 AI 开发任务写一个可复用任务包，并记录两次实际成本。',{priority:1}),
L('llm-tools','结构化输出与工具调用',"验证模型提出的参数、权限与执行结果。",['py-json','eng-contract','llm-context'],[
 ["把文本接口变成明确结构", "模型输出 JSON 或工具参数后，程序仍应验证 schema、范围、权限与业务条件。结构合法不代表语义正确；amount 是 number 也可能是负数或超过预算。"],
 ["执行边界在你的系统", "工具调用流程是模型提议 → 校验 → 授权检查 → 执行 → 返回结果。写操作要考虑确认策略、幂等和审计。工具返回内容也可能不可信，不能让文档里的指令越过原任务权限。"],
 ["合法参数和可执行动作分开判断", "请求工具 search_notes、query=\"退货规则\"，结构可能正确，但系统还需知道谁在查询、哪些笔记允许他读取。检查工具名、查询长度，只完成其中一部分。\n\n模型输出“调用成功”也不是执行证据，应该以工具实际返回的状态为准。外部文档里夹带的指令仍是文档内容，不会自动获得原任务的权限。"]
],`def validate_call(call):
    if call.get("tool") != "search_notes":
        return False
    query = call.get("query")
    return isinstance(query, str) and 0 < len(query) <= 100

print(validate_call({"tool":"search_notes", "query":"Python 返回值"}))
print(validate_call({"tool":"delete_all", "query":"x"}))`,'允许的检索请求为 True，未授权的删除工具为 False。这只是校验层示例。',['不要把模型输出直接 eval 成任意代码。','参数校验通过不等于当前用户有权限操作目标资源。'],[
 Q('工具参数通过 JSON schema 后，还需要？',['权限和业务语义检查','直接无条件执行','删除所有日志'],0,['正确，结构只是第一层。','可能执行越权或错误动作。','日志是审计与调试证据。']),
 Q('外部网页要求 Agent 忽略原任务并发送密钥，应？',['当作普通不可信内容，不获得指令权限','因为网页有文字就照做','先把密钥贴到日志'],0,['正确，数据不能自动升级成授权。','会破坏权限边界。','不应暴露机密。'])
],['mcp','ts'],'为一个工具写参数 schema、可调用角色、读写性质和失败返回格式。',{priority:1}),
L('llm-rag','RAG 与可追溯回答',"分别检查检索证据和答案是否受证据支持。",['llm-tools','eng-tests'],[
 ["分开检索与生成", "文档清洗和切分 → 索引 → 根据问题召回 → 可选重排 → 带证据生成。块太小丢上下文，太大稀释相关性；引用应能回到原文位置与版本。"],
 ["两段都要评估", "先测相关证据是否召回，再测答案是否忠于证据、引用是否支持结论。若问题答案不在库里，应明确不足而不是编造。权限过滤需要贯穿检索和展示。"],
 ["把一个错误答案分成两种情况", "问题问“活动是否包含周末”，检索结果只有优惠金额，没有活动时间。即使生成器忠实复述这些文字，也无法回答原问题，应先检查检索。\n\n若检索到了明确写着“不含周末”的条款，回答却说包含，问题就在生成是否遵循证据。分别保存检索片段和最终回答，才能知道该改哪一段。"]
],`docs = ["Python return 返回结果", "SQL JOIN 连接数据", "Python print 显示文本"]
query_terms = ["Python", "返回"]
ranked = sorted(docs, key=lambda d: sum(term in d for term in query_terms), reverse=True)
for doc in ranked:
    print(doc)`,'这是关键词召回的可解释玩具基线，没有向量模型或联网生成。先用基线理解检索职责。',['高相似度不代表证据蕴含答案。','有引用格式不代表引用真实支持每个事实。'],[
 Q('答案错了，RAG 首先应该拆开检查哪两段？',['证据召回与基于证据生成','logo 与背景色','只换更大模型'],0,['正确，找到错误发生在哪一段。','不能定位知识错误。','可能昂贵且没有解决根因。']),
 Q('库中没有答案时，合理行为是？',['说明证据不足','编出一段带引用的话','把最相似文本当事实'],0,['正确，必要时请求补充资料。','引用不能掩盖捏造。','相似不等于支持。'])
],['rag','rec'],'准备十个问题：有答案、跨文档、无答案、无权限各覆盖，并记录检索与回答错误。',{priority:1,minutes:45}),
L('llm-agent','Agent、MCP 与执行循环',"了解执行循环、MCP 接入和停止条件。",['llm-tools','eng-tests'],[
 ["Agent 是带反馈的任务执行", "模型决定下一步，系统执行工具、观察结果，再决定继续或停止。简单固定流程可能比自由循环更可靠。设置最大步数、超时、预算和失败出口，避免无限重试。"],
 ["MCP 解决接入协议", "MCP 定义客户端与服务器暴露工具、资源等能力的方式。它不自动保证工具质量、权限正确或任务成功；插件是能力打包与分发方式，也不能替代实现理解。协议版本和 SDK 要按实际环境核对。"],
 ["一次循环何时算完成", "查资料时，模型提出 search，程序执行并返回结果，模型再判断要不要 read 或结束。如果连续搜索没有新增信息，系统应有停止条件，而不是无限重试。\n\nMCP 提供能力接入的协议，不能代替这个执行循环的设计。接入一个工具后，还要检查它的输入、输出、权限和失败处理；工具数量本身不说明任务完成率。"]
],`actions = ["search", "read", "search", "finish"]
budget = 3
for step, action in enumerate(actions, 1):
    if step > budget:
        print("停止：达到步数预算")
        break
    print(step, action)`,'在第 4 个动作前停止。真实系统还要记录耗时、费用、调用状态和恢复点。',['接入更多工具会扩大选择与权限复杂度。','模型说“完成”不能代替外部系统的执行证据。'],[
 Q('MCP 接通后能自动保证什么？',['自动保证任务正确','提供协议层的能力接入方式','自动拥有所有系统权限'],1,['协议不保证业务结果。','正确，可靠性仍需额外设计。','权限需要显式授予和验证。']),
 Q('自主循环最基本的停止约束之一？',['最大步数或预算','无限失败重试','只要模型还想继续就继续'],0,['正确，配合超时与错误出口。','可能导致成本和副作用失控。','缺乏外部控制。'])
],['mcp'],'画出你负责的 Agent 的状态转换，指出每个写工具的权限检查与停止条件。',{priority:1,minutes:45}),
L('llm-eval','评估、成本与延迟',"用同一批任务比较质量、用量和耗时。",['llm-context','eng-tests'],[
 ["建立代表性任务集", "覆盖正常、边界、无答案、恶意输入、工具失败和恢复。保留输入、期望标准、系统版本和实际轨迹。程序断言、人工评审、模型评判各有适用范围；模型裁判也需校准。"],
 ["同时看质量和代价", "单次费用由输入、输出、缓存等实际规则决定；服务价格会变，示例不使用虚构现价。记录成功率、总费用、端到端延迟和重试分布，再决定模型路由或上下文压缩。"],
 ["把失败也计入总成本", "三次合成任务分别用了 800、2,000、900 tokens，中间一次失败。总量是 3,700，而不是只把成功的两次相加得到 1,700。\n\n要比较压缩上下文或换模型的效果，应在相同任务上同时记录成功、重试、耗时和用量。token 数不是直接费用，价格还取决于所用服务的输入、输出和缓存规则。"]
],`runs = [
    {"ok":True,"seconds":4,"tokens":800},
    {"ok":False,"seconds":10,"tokens":2000},
    {"ok":True,"seconds":5,"tokens":900}
]
print("样本成功率:", sum(r["ok"] for r in runs)/len(runs))
print("总 token:", sum(r["tokens"] for r in runs))`,'3 个合成样本成功率 2/3，总 token 3700；不足以推断生产效果。',['只报告平均延迟可能掩盖长尾失败。','测试集被反复针对优化也会过拟合，需要保留独立评估。'],[
 Q('压缩上下文省了 30% token，但成功率下降，应？',['结合任务质量和总成本权衡','仅因 token 少就上线','隐藏失败样本'],0,['正确，省钱必须有质量约束。','可能增加返工和业务损失。','会污染评估。']),
 Q('模型裁判的分数可直接当绝对真相吗？',['可以','不可以，应校准并检查偏差','比真实用户结果永远可靠'],1,['也会受提示与模型偏差影响。','正确，要与人工或可验证标准比较。','没有这种保证。'])
],['metrics','stats'],'从最近真实任务中建立 12 条评估案例，分别列出质量、成本与延迟标准。',{priority:1}),
L('llm-finetune','SFT、LoRA 与量化',"区分行为适配、知识检索和低秩参数更新。",['llm-tokens','dl-backprop','ml-splits'],[
 ["微调与检索各有用途", "SFT 用监督样本调整行为；RAG 给推理补充可更新的外部证据。知识常更新且需引用，先评估检索；输出格式或特定行为稳定需求，可考虑微调。两者也可以组合。"],
 ["LoRA 是低秩更新", "对权重增量用 ΔW=BA 表示，若 r 远小于输入输出维度，可显著减少可训练参数。量化降低数值精度和存储，可能影响质量与硬件效率；不能仅凭位数推断最终效果。"],
 ["算出 LoRA 减少了什么", "一个 1,024×1,024 权重矩阵有 1,048,576 个参数。若低秩增量的 rank=8，两个矩阵分别为 1,024×8 和 8×1,024，共 16,384 个可训练参数。\n\n减少的是适配时需要训练的这部分参数。基础模型仍要存储和前向计算，激活、优化器状态、批大小等也影响显存，所以不能按这个比例直接估计整次训练的显存下降。"]
],`d_in, d_out, rank = 1024, 1024, 8
full = d_in*d_out
lora = d_out*rank + rank*d_in
print("完整矩阵参数:", full)
print("LoRA新增可训练参数:", lora)
print("比值:", lora/full)`,'此矩阵新增 LoRA 参数为 16384，完整矩阵为 1048576；不等于整个训练显存按同样比例下降。',['LoRA 仍需存储和运行基础模型。','量化、蒸馏、剪枝与低秩微调是不同技术。'],[
 Q('LoRA 通常减少哪一部分？',['适配时需要训练的参数','所有基础模型都消失','数据标注工作必定归零'],0,['正确，基础权重通常冻结并保留。','推理仍依赖基础模型。','高质量训练数据仍重要。']),
 Q('知识每周更新且要求来源引用，通常先评估？',['检索增强方案','只把温度设低','完全忽略更新'],0,['正确，可更新证据与引用更契合需求。','温度不更新知识。','无法满足任务。'])
],['lora','llm'],'写一个是否需要微调的决策记录：目标、数据、基线、预算、评估与回滚。',{depth:'算法进阶',minutes:45}),
L('llm-alignment','RLHF、DPO 与 GRPO',"比较偏好数据、奖励信号和优势估计。",['rl-ppo','llm-finetune','math-info'],[
 ["RLHF 是一类流程", "常见路线先监督微调，再从偏好数据学奖励模型，最后用 PPO 等方式优化策略并约束偏离。奖励模型只是偏好代理，可能被利用，仍要独立检验真实任务质量。"],
 ["DPO 与 GRPO 的关键区别", "DPO 在其推导假设下直接从偏好对优化策略，典型训练不显式运行独立奖励模型加在线 RL 循环。GRPO 用同题一组输出的奖励构造相对优势，原始 DeepSeekMath 方案省去独立价值模型；它仍需要奖励信号。"],
 ["继续深入的验收标准", "能区分偏好数据、奖励、优势、参考策略与 KL 约束的角色；再按原文推导损失并复现实验。不能因为答对本页概念题就称为已经掌握大模型后训练。"],
 ["不要混淆奖励模型和价值模型", "奖励信号评估输出或动作的结果；价值模型估计状态下的预期后续回报，两者职责不同。原始 GRPO 用同题一组输出的奖励构造相对优势，省去独立价值模型，但仍需要奖励信号。\n\n奖励为 [1,0,1,0] 时，组均值 0.5、这里使用的总体标准差 0.5，标准化后约为 [1,−1,1,−1]。这里只算优势的一个环节，尚未包含完整优化目标。"]
],`rewards = [1.0, 0.0, 1.0, 0.0]
mean = sum(rewards)/len(rewards)
std = (sum((r-mean)**2 for r in rewards)/len(rewards))**0.5
advantages = [(r-mean)/(std+1e-8) for r in rewards]
print([round(a,3) for a in advantages])`,'组内标准化优势约 [1,−1,1,−1]。这是 GRPO 一个环节的玩具计算，不是完整目标。',['没有独立价值模型不等于没有奖励，也不等于无需评估。','偏好一致性、事实正确性和安全性需要分别测量。'],[
 Q('GRPO 原始方案省去独立价值模型，是否不再需要奖励？',['是','否，仍需奖励构造相对优势','只需 logo'],1,['优势计算仍依赖奖励信号。','正确，要分清奖励模型与价值模型。','与训练无关。']),
 Q('DPO 的典型训练数据是什么？',['提示与偏好回答对','只有任意图片尺寸','数据库索引清单'],0,['正确，用偏好差异直接优化策略目标。','与 DPO 核心数据形式不同。','不提供偏好监督。'])
],['dpo','grpo'],'对照原文整理 PPO-RLHF、DPO、GRPO 的数据、训练信号和计算组件。',{depth:'算法导览',minutes:60}),
];
