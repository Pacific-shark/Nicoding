import {lesson as L,question as Q,challenge as C} from './schema.js';
export const rl = [
L('rl-bandits','多臂老虎机与探索','当你还不知道哪个选择更好。',['math-prob'],[
 ['探索和利用的冲突','选历史平均奖励最高的选项是在利用；尝试了解不确定选项是在探索。ε-greedy 以 ε 概率随机探索，否则选当前估计最优项。它与固定分流实验的目标不同。'],
 ['UCB 把不确定性放进分数','常见 UCB 分数是均值加上随观察次数下降的置信奖励。少试过的选项因此可能被选择。公式依赖假设与版本，生产场景还要处理非平稳、约束和反馈延迟。']
],`import math
means = [0.6, 0.5]
counts = [100, 5]
t = sum(counts)
ucb = [m+math.sqrt(2*math.log(t)/n) for m,n in zip(means,counts)]
print([round(x,3) for x in ucb])
print("选择:", max(range(2), key=lambda i: ucb[i]))`,'选项 1 的均值较低，但不确定性加成大，示例会选它。分数可超过奖励上限，不是概率。',['先处理每个选项尚未观察的情况，避免除零。','在线优化策略会改变采样分布，分析不能照搬所有固定随机实验假设。'],[
 Q('ε-greedy 中 ε 主要控制什么？',['随机探索概率','网络层数','折扣金额'],0,['正确，其余时候利用当前估计最优项。','算法可用于无神经网络的表格场景。','不要与业务折扣混淆。']),
 Q('UCB 分数大于 1 是否必然错误？',['是，所有分数都是概率','不是，它是均值加探索奖励','表示点击率超过100%'],1,['UCB 指标不等同概率。','正确，不能按 CTR 解读。','不代表实际概率超过 1。'])
],['rl'],'为两种触达文案比较固定 A/B 与 bandit 的目标、探索成本和分析方式。',{depth:'算法进阶',minutes:45}),
L('rl-mdp','MDP、回报与 Bellman','当前动作还会改变未来处境。',['rl-bandits'],[
 ['五个要素','状态 S、动作 A、转移 P、奖励 R、折扣 γ 构成常见 MDP 描述。策略 π(a|s) 指定状态下怎样选动作；回报累积未来奖励。状态应包含预测未来所需的信息，否则可能是部分可观测问题。'],
 ['Bellman 拆开未来','Vπ(s)=E[r+γVπ(s′)] 把总价值分成一步奖励和后续价值。γ 接近 1 通常更看重远期，但不能代替明确的奖励设计。点击即时提高可能伤害长期满意度。']
],`rewards = [1, 0, 4]
gamma = 0.9
G = 0.0
for r in reversed(rewards):
    G = r + gamma*G
print(G)`,'回报 1+0.9×0+0.9²×4=4.24。倒序递推实现同一公式。',['奖励是训练信号，不自动等同用户真实利益。','状态和观察不一定相同，历史信息有时不可省略。'],[
 Q('回报与一步奖励有什么不同？',['回报考虑未来奖励累计','完全相同','回报只看最后的界面'],0,['正确，常带折扣。','多步任务中不同。','界面不是数学定义。']),
 Q('Bellman 关系把价值拆成？',['一步奖励与折扣后的后续价值','文件大小与像素','只剩当前奖励'],0,['正确，递归联系当前与未来。','与价值函数无关。','那会忽略未来。'])
],['rl'],'把一个连续决策场景写成状态、动作、奖励、终止条件，检查状态是否够用。',{depth:'算法进阶',challenge:C('实现 discounted_return(rewards, gamma)：返回从第一个奖励开始的折扣和，空列表返回 0。',`def discounted_return(rewards, gamma):
    return sum(rewards)`,`def discounted_return(rewards, gamma):
    total = 0
    for r in reversed(rewards):
        total = r + gamma * total
    return total`,['assert abs(discounted_return([1,0,4],0.9)-4.24)<1e-9','assert discounted_return([],0.9)==0','assert discounted_return([1,2],0)==1','assert discounted_return([1,2],1)==3'],['第 t 个奖励乘 gamma 的 t 次幂。','也可以从末尾倒序累积 total = r + gamma * total。'])}),
L('rl-q','Q-learning 与 SARSA','用一次经历修正动作价值。',['rl-mdp','math-opt'],[
 ['Q 表记录状态动作价值','Q(s,a) 估计先选动作 a 后的未来回报。Q-learning 更新：Q←Q+α[r+γ max_a′Q(s′,a′)−Q]。方括号里的差是 TD error。终止状态的后续价值为零。'],
 ['目标策略与行为策略','Q-learning 以贪心后续动作构造目标，行为仍可探索，属于 off-policy。SARSA 用实际选到的下个动作 Q(s′,a′)，是 on-policy 的典型算法。收敛结论需要探索与步长等条件。']
],`q, reward, gamma, next_max, alpha = 2.0, 1.0, 0.9, 4.0, 0.5
target = reward + gamma*next_max
td_error = target-q
q += alpha*td_error
print("目标/误差/新Q:", target, td_error, q)`,'目标 4.6，误差 2.6，新 Q=3.3。这是一次更新，不是收敛证明。',['终止后的 bootstrap 不应继续计入真实不存在的未来。','学习率 α 与折扣 γ 有不同意义。'],[
 Q('终止状态的更新目标通常是？',['即时奖励 r','r+无限未来','保持任意 next_max'],0,['正确，终止后无后续回报。','终止就是没有后续。','会错误引入不存在的价值。']),
 Q('SARSA 相较 Q-learning 的典型差别？',['使用实际下个动作的价值','永远不用奖励','没有状态'],0,['正确，而 Q-learning 常用最大后续 Q。','两者都依赖奖励。','两者都需要状态。'])
],['q'],'手算两个 Q 更新，并解释行为探索和目标贪心为什么可以不同。',{depth:'算法进阶',minutes:45}),
L('rl-dqn','DQN、回放与目标网络','用神经网络近似 Q 表。',['rl-q','dl-backprop'],[
 ['从查表到函数近似','输入状态，网络输出各离散动作的 Q 值。训练目标仍来自 TD 思路，但目标会随网络改变，样本又高度相关，训练可能不稳定。'],
 ['两个关键机制','经验回放存储转移并打散抽样；目标网络在一段时间内提供较稳定的 bootstrap 估计。DQN 不是只把表替换成神经网络就完成。还需区分真正终止与时间截断，决定是否 bootstrap。']
],`transitions = [
    {"r":1, "terminated":False, "next_q":4},
    {"r":1, "terminated":True, "next_q":4}
]
for item in transitions:
    target = item["r"] + 0.9*(not item["terminated"])*item["next_q"]
    print(target)`,'非终止目标 4.6；终止目标 1.0。时间截断不能机械当作环境终止。',['回放池数据也可能来自旧策略或不同分布。','普通 DQN 面向离散动作；连续控制通常需要别的设计。'],[
 Q('目标网络主要帮助什么？',['让 bootstrap 目标相对稳定','生成真实标签','完全消除训练随机性'],0,['正确，减少目标随每次更新立刻漂移。','目标仍是估计。','训练仍有随机性和不稳定风险。']),
 Q('经验回放的一项作用？',['打破连续样本的强相关并复用经验','让奖励永远为正','把环境变成静态图片'],0,['正确，但仍需合理采样与容量。','不会修改任务奖励定义。','存储的是转移数据。'])
],['dqn'],'按 PyTorch 教程跑 CartPole，记录回放池、目标网络和终止标志各在何处使用。',{depth:'算法进阶',minutes:60}),
L('rl-policy','策略梯度与 Actor–Critic','直接学习怎样选动作。',['rl-mdp','dl-backprop','math-info'],[
 ['策略可以直接输出概率','策略梯度用回报或优势加权 log π(a|s) 的梯度：好于预期的动作提高相对概率，差于预期的动作降低。采样带来方差，基线可以帮助降低它。'],
 ['Actor 与 Critic 分工','Actor 表示策略，Critic 估计价值帮助形成训练信号。优势 A≈回报−基线 衡量相对预期，而不是绝对奖励。Critic 也有误差，因此不能把其输出视为真实价值。']
],`returns = [5.0, 1.0, 3.0]
baseline = 3.0
advantages = [g-baseline for g in returns]
print(advantages)
print(["提高倾向" if a>0 else "降低倾向" if a<0 else "此项零信号" for a in advantages])`,'优势 [2,−2,0]；这是更新方向直觉，完整梯度还依赖策略参数化。',['正奖励也可能低于基线，从而得到负优势。','baseline 的使用需满足相应估计条件，不是随意改奖励。'],[
 Q('回报为 2、基线为 3，优势为？',['−1','2','5'],0,['正确，2−3=−1。','没有减去基线。','把差写成和了。']),
 Q('Critic 的角色通常是？',['估计价值以辅助训练信号','直接充当真实环境','保证策略永远最优'],0,['正确，其估计也可能有误差。','环境转移来自环境或另学的模型。','没有这样的保证。'])
],['rl'],'解释为何一个有正奖励的动作仍可能被降低概率，并给出数字例子。',{depth:'算法进阶',minutes:45}),
L('rl-ppo','PPO 与 SAC','限制策略变化，或鼓励适量随机性。',['rl-policy','math-info'],[
 ['PPO 的局部改进直觉','概率比 r=π_new(a|s)/π_old(a|s)，优势 A 衡量动作好坏。PPO-Clip 使用 min(rA, clip(r,1−ε,1+ε)A) 抑制某些过大更新的收益。裁剪代理目标不等于严格限制所有状态的 KL。'],
 ['SAC 的最大熵目标','SAC 在回报中加入熵奖励，学习高收益同时保持随机性的策略，常用于连续控制并利用离策略数据。熵系数管理奖励与探索的取舍。PPO 和 SAC 的数据复用、动作空间与计算成本都要比较。']
],`ratio, advantage, epsilon = 1.4, 2.0, 0.2
clipped = min(max(ratio, 1-epsilon), 1+epsilon)
objective = min(ratio*advantage, clipped*advantage)
print(ratio*advantage, objective)`,'原代理项 2.8，裁剪后取 2.4。把 advantage 改成 −2，观察 min 的不同效果。',['PPO 的 clip 不是对参数直接截断。','增加熵不保证更高最终收益，系数和任务有关系。'],[
 Q('PPO-Clip 裁剪的核心对象是？',['新旧策略动作概率比的代理项','所有网络权重到零','环境状态坐标'],0,['正确，不是直接截断网络参数。','这会毁掉模型。','不是目标定义。']),
 Q('SAC 目标中的熵项通常鼓励？',['策略保持一定随机性','所有动作永远等概率','不再看奖励'],0,['正确，与回报一起权衡。','奖励仍影响策略，未必均匀。','目标同时考虑回报。'])
],['ppo','sac'],'用正、负优势各计算一次 PPO 裁剪目标，再解释为何两种情况要分别看。',{depth:'算法进阶',minutes:60}),
L('rl-offline','离线 RL、模型学习与多智能体','知道算法在哪些条件下容易失效。',['rl-dqn','rl-ppo','ml-splits'],[
 ['离线数据无法随时补试','离线 RL 从固定日志学习，若策略偏向日志少见动作，价值可能严重高估。保守价值估计或策略约束处理分布外风险，但不是离线最优性的通用保证。'],
 ['学环境，也会学错环境','基于模型的方法学习转移与奖励，再规划或生成经验；多步预测会累积模型误差。多智能体中其他策略也在变化，使单个智能体看到的环境可能非平稳。'],
 ['这是一张继续深入的路线图','先完成单智能体小环境，再选离线数据集、模型学习或多智能体中的一个方向复现。需要记录数据覆盖、评估协议、随机种子与失败结果；这关测验只检查核心区别。']
],`logged_actions = {"A":1000, "B":2, "C":0}
estimated_q = {"A":2.0, "B":5.0, "C":10.0}
for action in estimated_q:
    print(action, "估计Q", estimated_q[action], "日志次数", logged_actions[action])`,'C 的估计最高却无日志支持；表中 Q 是人为示例，不能据此认定 C 最优。',['固定日志的覆盖不足无法靠换大网络自动弥补。','模拟器高分不等于真实环境可靠。'],[
 Q('离线策略选择日志中几乎没有的动作，主要担心？',['分布外价值估计误差','动作名字太短','日志一定更准确'],0,['正确，缺少支持数据可使估计失真。','与可靠性无关。','无法由此推出。']),
 Q('多智能体同时学习时，环境可能？',['对单个智能体呈现非平稳性','必定完全静止','不再需要奖励'],0,['正确，其他智能体策略也在变。','与共同学习矛盾。','优化仍需目标。'])
],['cql','rl'],'选择一个深入方向，写清可获得的数据、评估环境和你无法验证的假设。',{depth:'算法导览',minutes:60}),
];
